# 🔧 Исправление ошибки 403 Forbidden для видео файлов

Ошибка 403 Forbidden при доступе к видео файлам обычно связана с:

1. Неправильными правами доступа к файлам/директории
2. Неправильной конфигурацией Nginx
3. Неправильным путем в `alias` директивы Nginx

## 🔍 Диагностика

### 1. Проверьте права доступа к директории и файлам

На сервере выполните:

```bash
# Проверьте права на директорию videos
ls -la /root/WebConvert/webapp/videos/

# Проверьте права на конкретный файл
ls -la /root/WebConvert/webapp/videos/имя_файла.mp4
```

**Правильные права:**

- Директория `videos/` должна иметь права `755` (drwxr-xr-x)
- Файлы должны иметь права `644` (-rw-r--r--)
- **ВАЖНО:** Все родительские директории (`/root`, `/root/WebConvert`, `/root/WebConvert/webapp`) должны иметь права на выполнение (`x`) для других пользователей, иначе Nginx не сможет получить доступ к файлам

### 2. Проверьте конфигурацию Nginx

```bash
# Откройте конфигурацию Nginx
sudo nano /etc/nginx/sites-available/video-upload

# Проверьте синтаксис
sudo nginx -t

# Посмотрите логи ошибок
sudo tail -f /var/log/nginx/error.log
```

### 3. Проверьте, что путь в Nginx правильный

В конфигурации Nginx должна быть секция:

```nginx
location /videos/ {
    alias /root/WebConvert/webapp/videos/;

    # Заголовки для видео
    add_header Content-Type video/mp4;
    add_header Accept-Ranges bytes;

    # Кэширование
    expires 30d;
    add_header Cache-Control "public, immutable";

    # CORS
    add_header Access-Control-Allow-Origin *;
}
```

**Важно:**

- Путь в `alias` должен заканчиваться на `/`
- Путь должен быть абсолютным (начинаться с `/`)
- Директория должна существовать

## 🚀 Быстрое решение (пошагово)

Выполните эти команды на сервере по порядку:

```bash
# 1. Проверьте, что директория существует
ls -la /root/WebConvert/webapp/videos/

# 2. Установите правильные права на директорию
sudo chmod 755 /root/WebConvert/webapp/videos

# 3. Установите права на все файлы
sudo chmod 644 /root/WebConvert/webapp/videos/*

# 4. Установите владельца (если нужно)
sudo chown -R root:root /root/WebConvert/webapp/videos

# 5. Проверьте конфигурацию Nginx
sudo nano /etc/nginx/sites-available/video-upload

# 6. Убедитесь, что в секции /videos/ путь правильный:
#    alias /root/WebConvert/webapp/videos/;

# 7. Проверьте синтаксис
sudo nginx -t

# 8. Перезапустите Nginx
sudo systemctl restart nginx

# 9. Проверьте логи
sudo tail -f /var/log/nginx/error.log
```

## 🔧 Детальное решение

### Шаг 1: Исправьте права доступа

```bash
# Установите правильные права на директорию
sudo chmod 755 /root/WebConvert/webapp/videos

# Установите правильные права на все файлы в директории
sudo chmod 644 /root/WebConvert/webapp/videos/*

# Убедитесь, что владелец правильный (обычно root или www-data)
sudo chown -R root:root /root/WebConvert/webapp/videos

# ВАЖНО: Убедитесь, что родительские директории имеют права на выполнение
# Это необходимо для того, чтобы Nginx мог получить доступ к файлам
sudo chmod 755 /root
sudo chmod 755 /root/WebConvert
sudo chmod 755 /root/WebConvert/webapp
```

**Примечание:** Если вы используете другого пользователя (не root), замените `/root` на правильный путь.

**Если используете другого пользователя:**

```bash
# Замените root на ваше имя пользователя
sudo chown -R ваш-пользователь:ваш-пользователь /root/WebConvert/webapp/videos
```

### Шаг 2: Проверьте и исправьте конфигурацию Nginx

```bash
# Откройте конфигурацию
sudo nano /etc/nginx/sites-available/video-upload
```

Убедитесь, что секция `/videos/` выглядит так:

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Максимальный размер загружаемого файла (2GB)
    client_max_body_size 2G;
    client_body_timeout 300s;

    # Проксирование на backend
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты для больших файлов
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Раздача статических видео файлов
    location /videos/ {
        alias /root/WebConvert/webapp/videos/;

        # ВАЖНО: Отключаем проверку индексов
        autoindex off;

        # Заголовки для видео
        add_header Content-Type video/mp4;
        add_header Accept-Ranges bytes;

        # Кэширование
        expires 30d;
        add_header Cache-Control "public, immutable";

        # CORS
        add_header Access-Control-Allow-Origin *;

        # Разрешаем доступ к файлам
        access_log off;
    }
}
```

**Критически важно:**

- Путь в `alias` должен быть **абсолютным** и **заканчиваться на `/`**
- Если ваша директория находится в другом месте, замените `/root/WebConvert/webapp/videos/` на правильный путь
- Убедитесь, что путь существует: `ls -la /root/WebConvert/webapp/videos/`

### Шаг 3: Проверьте синтаксис и перезапустите Nginx

```bash
# Проверьте синтаксис
sudo nginx -t

# Если синтаксис правильный, перезапустите Nginx
sudo systemctl restart nginx

# Проверьте статус
sudo systemctl status nginx
```

### Шаг 4: Проверьте доступность

```bash
# Проверьте, что файл существует
ls -la /root/WebConvert/webapp/videos/имя_файла.mp4

# Проверьте доступ через curl (замените на ваш домен и имя файла)
curl -I https://ваш-домен.com/videos/имя_файла.mp4
```

Должен вернуться статус `200 OK`, а не `403 Forbidden`.

## 🔍 Дополнительная диагностика

### Проверьте логи Nginx

```bash
# Логи ошибок
sudo tail -f /var/log/nginx/error.log

# Логи доступа
sudo tail -f /var/log/nginx/access.log
```

### Проверьте SELinux (если используется)

```bash
# Проверьте статус SELinux
getenforce

# Если включен, временно отключите для теста
sudo setenforce 0

# Если это помогло, настройте SELinux правильно
sudo setsebool -P httpd_read_user_content 1
```

### Альтернативное решение: использовать proxy_pass вместо alias

Если `alias` не работает, можно использовать `proxy_pass`:

```nginx
location /videos/ {
    # Удаляем /videos/ из пути и проксируем на backend
    rewrite ^/videos/(.*)$ /videos/$1 break;
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

Но это менее эффективно, так как запросы будут идти через FastAPI backend.

## ✅ Проверка после исправления

1. Загрузите новое видео через Web App
2. Скопируйте ссылку из списка видео
3. Откройте ссылку в браузере
4. Видео должно открыться или начать загружаться

Если проблема сохраняется, проверьте:

- Правильность пути в `alias`
- Права доступа к файлам и директории
- Логи Nginx для детальной информации об ошибке
