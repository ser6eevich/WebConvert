# 🔧 Исправление ошибки "FileNotFoundError: [Errno 2] No such file or directory: 'ffprobe'"

Эта ошибка означает, что `ffprobe` не найден в системе. `ffprobe` - это утилита, которая идет вместе с FFmpeg и используется для анализа видео файлов.

## 🔍 Диагностика

### 1. Проверьте, установлен ли ffprobe

На сервере выполните:

```bash
# Проверьте наличие ffprobe
which ffprobe

# Проверьте версию
ffprobe -version

# Проверьте, что ffmpeg тоже установлен
which ffmpeg
ffmpeg -version
```

**Ожидаемый результат:**

- `which ffprobe` должен вернуть путь (например: `/usr/bin/ffprobe`)
- `ffprobe -version` должен показать версию

**Если команды не найдены:**

- FFmpeg не установлен или не в PATH
- Нужно установить FFmpeg

## 🔧 Решение

### Шаг 1: Установите FFmpeg (если не установлен)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y ffmpeg

# CentOS/RHEL
sudo yum install -y ffmpeg

# Проверьте установку
ffmpeg -version
ffprobe -version
```

### Шаг 2: Проверьте, что ffprobe доступен

```bash
# Проверьте путь
which ffprobe

# Проверьте, что он работает
ffprobe -version
```

### Шаг 3: Если ffprobe установлен, но не в PATH

Если `ffprobe` установлен, но `which ffprobe` не находит его:

```bash
# Найдите ffprobe в системе
find /usr -name ffprobe 2>/dev/null
find /opt -name ffprobe 2>/dev/null

# Если найден, добавьте его директорию в PATH
# Например, если найден в /usr/local/bin/ffprobe:
export PATH=/usr/local/bin:$PATH

# Для постоянного добавления в PATH, добавьте в ~/.bashrc или ~/.profile:
echo 'export PATH=/usr/local/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

### Шаг 4: Перезапустите бота

```bash
# Перезапустите сервис бота
sudo systemctl restart WebConvert

# Проверьте логи
sudo journalctl -u WebConvert -f
```

## 🔍 Дополнительная диагностика

### Проверьте переменные окружения в systemd

Если бот запущен через systemd, проверьте, что PATH правильный:

```bash
# Проверьте service файл
sudo cat /etc/systemd/system/WebConvert.service

# Убедитесь, что в Environment есть PATH или добавьте:
# Environment="PATH=/usr/bin:/usr/local/bin:/bin"
```

### Проверьте логи бота

```bash
# Посмотрите последние логи
sudo journalctl -u WebConvert -n 50

# Ищите сообщения о ffprobe
sudo journalctl -u WebConvert | grep -i ffprobe
```

## ✅ Проверка после исправления

1. **Проверьте установку:**

   ```bash
   which ffmpeg
   which ffprobe
   ffmpeg -version
   ffprobe -version
   ```

2. **Перезапустите бота:**

   ```bash
   sudo systemctl restart WebConvert
   ```

3. **Проверьте логи:**

   ```bash
   sudo journalctl -u WebConvert -f
   ```

4. **Протестируйте конвертацию:**
   - Отправьте боту видео
   - Проверьте, что конвертация работает без ошибок

## 🐛 Если проблема сохраняется

1. **Проверьте права доступа:**

   ```bash
   ls -la $(which ffprobe)
   ```

2. **Проверьте, что ffprobe исполняемый:**

   ```bash
   file $(which ffprobe)
   ```

3. **Попробуйте запустить ffprobe напрямую:**

   ```bash
   /usr/bin/ffprobe -version
   ```

4. **Если нужно, укажите явный путь в .env:**
   ```env
   # В файле .env добавьте (если ffprobe не в PATH):
   # Но обычно это не нужно, если FFmpeg установлен системно
   ```

## 📝 Примечания

- `ffprobe` обычно устанавливается вместе с `ffmpeg`
- На Ubuntu/Debian: `sudo apt install ffmpeg` устанавливает и `ffmpeg`, и `ffprobe`
- Если вы установили FFmpeg из исходников, убедитесь, что `ffprobe` тоже скомпилирован
- Библиотека `ffmpeg-python` использует `ffprobe` для анализа видео перед конвертацией
