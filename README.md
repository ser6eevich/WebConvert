# Бот-помощник для работы

Telegram бот с двумя основными функциями:

1. **Конвертация видео** - автоматически конвертирует присланные видео в MP4 1920x1080
2. **Генерация текста поста** - создает текст для поста из присланного текста или файла с использованием GPT

## Установка

1. Установите зависимости:

```bash
# На Windows используйте:
py -m pip install -r requirements.txt

# Или на Linux/macOS:
pip install -r requirements.txt
```

2. Установите FFmpeg:

   **Windows (подробная инструкция):**

   📖 **Полная пошаговая инструкция доступна в файле [FFMPEG_INSTALL_WINDOWS.md](FFMPEG_INSTALL_WINDOWS.md)**

   Краткая версия:

   1. Скачайте FFmpeg:

      - Перейдите на https://www.gyan.dev/ffmpeg/builds/ (рекомендуется) или https://ffmpeg.org/download.html
      - Скачайте файл `ffmpeg-release-essentials.zip` (или последнюю версию)
      - Распакуйте архив в удобное место, например: `C:\ffmpeg`

   2. Добавьте FFmpeg в PATH:

      - Нажмите `Win + R`, введите `sysdm.cpl` и нажмите Enter
      - Или: Правой кнопкой на "Этот компьютер" → "Свойства" → "Дополнительные параметры системы"
      - Откройте вкладку "Дополнительно"
      - Нажмите кнопку "Переменные среды"
      - В разделе "Системные переменные" найдите переменную `Path` и нажмите "Изменить"
      - Нажмите "Создать" и добавьте путь к папке `bin` внутри распакованной папки FFmpeg
        (например: `C:\ffmpeg\ffmpeg-6.x-essentials_build\bin`)
      - Нажмите "ОК" во всех открытых окнах

   3. Проверьте установку:
      - Откройте новое окно PowerShell или командной строки
      - Выполните команду: `ffmpeg -version`
      - Если вы видите информацию о версии FFmpeg - установка прошла успешно!

   **Альтернативный способ (через Chocolatey):**

   ```powershell
   choco install ffmpeg
   ```

   **Linux:**

   ```bash
   sudo apt install ffmpeg
   ```

   **macOS:**

   ```bash
   brew install ffmpeg
   ```

3. Создайте файл `.env` в корне проекта и заполните:

   ```env
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
   OPENAI_API_KEY=your_openai_api_key_here
   GPT_ASSISTANT_ID=your_gpt_assistant_id_here
   GPT_ASSISTANT_ID_VIDEOS=your_gpt_assistant_id_for_videos_here
   TELEGRAM_PROXY_URL=  # Опционально: http://proxy.example.com:8080 (если Telegram заблокирован)
   TELEGRAM_LOCAL_API_URL=  # Опционально: http://localhost:8081 (если используется локальный Bot API сервер)
   FFMPEG_PATH=  # Опционально: C:\ffmpeg\bin\ffmpeg.exe (если FFmpeg не в PATH)
   ```

   - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
   - `OPENAI_API_KEY` - ваш API ключ OpenAI
   - `GPT_ASSISTANT_ID` - ID вашего GPT ассистента для постов вебинаров (обязательно)
   - `GPT_ASSISTANT_ID_VIDEOS` - ID вашего GPT ассистента для постов роликов на платформе (обязательно)
   - `TELEGRAM_PROXY_URL` - (опционально) URL прокси-сервера, если Telegram заблокирован в вашем регионе
   - `TELEGRAM_LOCAL_API_URL` - (опционально) URL локального Bot API сервера (например: `http://localhost:8081`) для работы с файлами до 2GB
   - `FFMPEG_PATH` - (опционально) Полный путь к `ffmpeg.exe`, если FFmpeg не добавлен в PATH

## Запуск

```bash
# На Windows:
py bot.py

# Или на Linux/macOS:
python bot.py
```

## Получение токенов

### Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен и добавьте в `.env` файл

### OpenAI API Key

1. Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ и добавьте в `.env` файл

### GPT Assistant ID

📖 **Подробная инструкция доступна в файле [GPT_ASSISTANT_SETUP.md](GPT_ASSISTANT_SETUP.md)**

Краткая версия:

1. Перейдите на [OpenAI Platform - Assistants](https://platform.openai.com/assistants)
2. Создайте нового ассистента
3. Настройте ассистента с инструкциями для обработки транскрибаций (см. GPT_ASSISTANT_SETUP.md)
4. Скопируйте ID ассистента (находится в URL после `/assistants/`, начинается с `asst_`)
5. Добавьте ID в `.env` файл как `GPT_ASSISTANT_ID=asst_xxxxxxxxxxxxx`

## Использование

### Конвертация видео

**Способ 1: Отправка видео (до 2GB)**

- Выберите режим "Конвертер" → "Отправить видео"
- ⚠️ **ВАЖНО:** Отправляйте видео как видео (через кнопку 'Видео' в Telegram), а не как файл!
- Отправьте видео боту (любой формат: MP4, MOV, AVI, WEBM, MKV и др.)
- Бот автоматически определит видео и начнет конвертацию
- Результат будет отправлен в формате MP4 с разрешением 1920x1080
- Видео масштабируется с сохранением пропорций, черные полосы добавляются при необходимости
- **Почему как видео?** Видео до 2GB можно отправить как видео, а файлы (документы) ограничены 50MB

**Способ 2: По ссылке (любой размер)**

- Выберите режим "Конвертер"
- Загрузите видео в облачное хранилище (Google Drive, Dropbox и т.д.)
- Получите прямую ссылку на файл
- Отправьте ссылку боту
- Бот скачает видео и конвертирует его через FFmpeg
- Результат будет отправлен вам в формате MP4 1920x1080
- **Преимущества:** Работает с файлами любого размера, не ограничено 50MB

### Генерация текста для поста

1. Выберите режим "Генерация"
2. Выберите тип поста:
   - **📝 Пост для вебинара** - использует первого ассистента (GPT_ASSISTANT_ID)
   - **🎬 Пост для роликов на платформе** - использует второго ассистента (GPT_ASSISTANT_ID_VIDEOS)
3. Отправьте транскрибацию:
   - **Вариант 1**: Текстовое сообщение с транскрибацией
   - **Вариант 2**: Текстовый файл (.txt, .doc, .docx, .md) с транскрибацией
4. Бот отправит транскрибацию соответствующему GPT ассистенту для обработки
5. Ассистент сгенерирует ответ, который будет разбит на 4 части:
   - **Название вебинара/ролика** - название материала
   - **Описание** - краткое описание содержания
   - **Тайм-код** - важные временные метки
   - **Пост для телеграм** - готовый текст для публикации
6. Каждая часть будет отправлена отдельным сообщением
7. **Важные моменты автоматически выделяются жирным текстом**

## Поддерживаемые форматы

**Видео:**

- MP4, MOV, AVI, WEBM, MKV и другие форматы, поддерживаемые FFmpeg

**Текстовые файлы:**

- .txt, .doc, .docx, .md

## Решение проблем

### Ошибка подключения к Telegram (TimedOut)

Если вы видите ошибку `TimedOut` или `ConnectTimeout`:

1. **Проверьте интернет-соединение**

   - Убедитесь, что у вас есть доступ в интернет
   - Попробуйте открыть https://web.telegram.org в браузере

2. **Telegram заблокирован в вашем регионе**

   - Используйте VPN или прокси-сервер
   - Добавьте в `.env` файл:
     ```env
     TELEGRAM_PROXY_URL=http://your-proxy-server:port
     ```
   - Примеры форматов прокси:
     - HTTP: `http://proxy.example.com:8080`
     - SOCKS5: `socks5://proxy.example.com:1080`

3. **Проверьте файрвол/антивирус**

   - Убедитесь, что файрвол не блокирует Python
   - Попробуйте временно отключить антивирус для проверки

4. **Проверьте токен бота**
   - Убедитесь, что токен правильный в файле `.env`
   - Получите новый токен у @BotFather, если нужно

### FFmpeg не найден

**Проблема:** Бот не находит FFmpeg, хотя вы его установили.

**Решения:**

1. **Перезапустите терминал/IDE**

   - После добавления FFmpeg в PATH нужно закрыть и открыть заново все окна терминала/IDE
   - Перезапустите бота после этого

2. **Проверьте правильность пути в PATH**

   - Убедитесь, что вы добавили путь к папке `bin` (где находится `ffmpeg.exe`)
   - Например: `C:\ffmpeg\ffmpeg-6.x-essentials_build\bin`
   - НЕ добавляйте путь к корневой папке!

3. **Укажите путь вручную (быстрое решение)**

   - Добавьте в файл `.env`:
     ```env
     FFMPEG_PATH=C:\ffmpeg\ffmpeg-6.x-essentials_build\bin\ffmpeg.exe
     ```
   - Замените путь на ваш реальный путь к `ffmpeg.exe`
   - Перезапустите бота

4. **Проверьте установку**
   - Откройте новое окно PowerShell
   - Выполните: `ffmpeg -version`
   - Если команда работает - FFmpeg установлен правильно

📖 **Подробная инструкция:** См. файл [FFMPEG_INSTALL_WINDOWS.md](FFMPEG_INSTALL_WINDOWS.md)

## 🎬 Загрузка видео через Web App

Бот поддерживает загрузку видео файлов любого размера (больше 50MB) через Telegram Web App.

### Настройка Web App

1. **Настройте backend сервер** (см. `WEBAPP_DEPLOY.md`)
2. **Добавьте в `.env` файл:**
   ```env
   VIDEO_WEBAPP_URL=https://your-domain.com/upload
   ```
3. **Перезапустите бота**

После настройки в главном меню появится кнопка "🎬 Загрузить видео", которая откроет Web App для загрузки видео.

**📚 Инструкции по развертыванию:**

- **🎯 Пошаговая инструкция (рекомендуется):** `DEPLOY_STEP_BY_STEP.md` - от локального тестирования до развертывания с доменом
- **Для начинающих (полная инструкция с нуля):** `DEPLOY_FOR_BEGINNERS.md` - как получить сервер, домен и всё настроить
- **Для опытных пользователей:** `WEBAPP_DEPLOY.md` - краткая инструкция по настройке Web App backend
- **Локальное тестирование:** `LOCAL_TESTING.md` - как протестировать через ngrok

## Примечания

- **Максимальный размер видео файла: 2GB (при отправке как видео)** ⚠️

  - ⚠️ **ВАЖНО:** Отправляйте видео как видео (через кнопку 'Видео'), а не как файл!
  - Видео, отправленные как видео, могут быть до 2GB
  - Файлы (документы) ограничены 50MB
  - Если вы отправите видео как файл, бот попросит отправить его как видео

  **Почему важно отправлять как видео:**

  - Telegram Bot API позволяет отправлять видео до 2GB
  - Файлы (документы) ограничены 50MB
  - Это ограничение установлено на уровне серверов Telegram
  - Отправка как видео позволяет обойти ограничение в 50MB

- Конвертация видео может занять некоторое время в зависимости от размера файла
  - Для больших файлов (500MB-2GB) конвертация может занять 10-30 минут
  - Используется быстрый preset для ускорения обработки
  - Все ядра процессора используются для параллельной обработки
  - Бот автоматически удаляет временные файлы после обработки
  - Таймауты подключения увеличены до 30 секунд для стабильной работы
  - **Важно о размерах файлов:**
    - Видео до 500MB: обрабатываются быстро (3-10 минут)
    - Видео 500MB-1GB: требуют больше времени (10-20 минут)
    - Видео 1GB-2GB: могут занять 20-30 минут
    - Файлы >2GB: используйте конвертацию по ссылке (отправьте прямую ссылку на видео файл)
