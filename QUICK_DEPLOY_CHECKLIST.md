# ✅ Чеклист развертывания (краткая памятка)

Используйте этот чеклист для быстрой проверки всех шагов. Подробные инструкции см. в `DEPLOY_FOR_BEGINNERS.md`.

## 📋 Что нужно сделать

### 1. Подготовка

- [ ] Арендован VPS сервер (DigitalOcean, Hetzner и т.д.)
- [ ] Получен IP адрес сервера
- [ ] Куплен домен (Namecheap, GoDaddy и т.д.)
- [ ] Настроены DNS записи (A-запись на IP сервера)
- [ ] Подключение к серверу работает (`ssh root@IP`)

### 2. Настройка сервера

- [ ] Система обновлена (`apt update && apt upgrade`)
- [ ] Создан пользователь `botuser`
- [ ] Установлены пакеты: `python3`, `nginx`, `certbot`
- [ ] Настроен файрвол (UFW): порты 22, 80, 443 открыты

### 3. Установка бота

- [ ] Файлы бота скопированы на сервер
- [ ] Создано виртуальное окружение
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан `.env` файл с токенами
- [ ] Бот запускается вручную (тест)

### 4. Установка Web App backend

- [ ] Папка `webapp/` скопирована на сервер
- [ ] Создано виртуальное окружение для backend
- [ ] Установлены зависимости (`pip install -r webapp/requirements.txt`)
- [ ] Создана директория `videos/`
- [ ] Создан `.env` файл с настройками
- [ ] Backend запускается вручную (тест)

### 5. Настройка Nginx

- [ ] Создан конфиг `/etc/nginx/sites-available/video-upload`
- [ ] Конфиг активирован (симлинк в `sites-enabled`)
- [ ] Nginx перезапущен (`sudo systemctl restart nginx`)
- [ ] Конфигурация проверена (`sudo nginx -t`)

### 6. SSL сертификат

- [ ] Получен SSL сертификат (`sudo certbot --nginx`)
- [ ] HTTPS работает (проверено в браузере)
- [ ] Автообновление сертификата настроено

### 7. Автозапуск

- [ ] Создан service для бота (`/etc/systemd/system/telegram-bot.service`)
- [ ] Создан service для backend (`/etc/systemd/system/video-upload-webapp.service`)
- [ ] Сервисы включены и запущены
- [ ] Статус проверен (`sudo systemctl status`)

### 8. Финальная проверка

- [ ] Бот отвечает в Telegram
- [ ] Кнопка "🎬 Загрузить видео" появляется
- [ ] Web App открывается при нажатии
- [ ] Загрузка видео работает
- [ ] Ссылка на видео отправляется в бот

## 🔧 Полезные команды

### Проверка статуса

```bash
sudo systemctl status telegram-bot
sudo systemctl status video-upload-webapp
sudo systemctl status nginx
```

### Просмотр логов

```bash
# Логи бота
sudo journalctl -u telegram-bot -f

# Логи backend
sudo journalctl -u video-upload-webapp -f

# Логи Nginx
sudo tail -f /var/log/nginx/video-upload-error.log
```

### Перезапуск сервисов

```bash
sudo systemctl restart telegram-bot
sudo systemctl restart video-upload-webapp
sudo systemctl restart nginx
```

### Проверка портов

```bash
sudo netstat -tulpn | grep 8000  # Backend
sudo netstat -tulpn | grep 80    # HTTP
sudo netstat -tulpn | grep 443   # HTTPS
```

## 🆘 Если что-то не работает

1. **Проверьте логи** (см. команды выше)
2. **Проверьте, что сервисы запущены**: `sudo systemctl status`
3. **Проверьте конфигурацию Nginx**: `sudo nginx -t`
4. **Проверьте DNS**: https://dnschecker.org/
5. **Проверьте файрвол**: `sudo ufw status`

## 📚 Документация

- **Для начинающих:** `DEPLOY_FOR_BEGINNERS.md` - полная инструкция с нуля
- **Для опытных:** `WEBAPP_DEPLOY.md` - краткая инструкция по Web App
- **Быстрый старт:** `WEBAPP_QUICKSTART.md` - локальное тестирование
