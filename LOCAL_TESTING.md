# 🧪 Локальное тестирование Web App

## ✅ Сервер запущен!

Ваш backend сервер успешно работает на `http://localhost:8000`.

## ⚠️ Важно для Telegram Web App

Telegram **требует HTTPS** для работы Web App. Локальный `http://localhost:8000` не подойдет.

## 🔧 Решение: Использование ngrok (для тестирования)

### 1. Установите ngrok

**Windows:**

- Скачайте: https://ngrok.com/download
- Распакуйте `ngrok.exe` в папку (например, `C:\ngrok\`)
- Или используйте через Chocolatey: `choco install ngrok`

**Linux/Mac:**

```bash
# Через Homebrew (Mac)
brew install ngrok

# Или скачайте с https://ngrok.com/download
```

### 2. Зарегистрируйтесь на ngrok

1. Перейдите на https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (можно через GitHub)
3. Скопируйте ваш **authtoken** из дашборда

### 3. Настройте ngrok

```bash
ngrok config add-authtoken ВАШ_AUTHTOKEN
```

### 4. Запустите туннель

В **новом** терминале (не закрывая backend сервер):

```bash
ngrok http 8000
```

Вы увидите что-то вроде:

```
Forwarding  https://abc123.ngrok-free.app -> http://localhost:8000
```

**Скопируйте HTTPS URL** (например: `https://abc123.ngrok-free.app`)

### 5. Обновите настройки

#### A. Обновите `.env` файл backend

Создайте или отредактируйте файл `webapp/.env`:

```env
# Используйте HTTPS URL от ngrok
PUBLIC_BASE_URL=https://abc123.ngrok-free.app

# Директория для видео
VIDEOS_DIR=videos

# Порт
WEBAPP_PORT=8000

# Максимальный размер (2GB)
MAX_FILE_SIZE=2147483648
```

#### B. Обновите `.env` файл бота

В корне проекта (где `bot.py`), обновите `.env`:

```env
# ... другие переменные ...

# URL для Web App (используйте HTTPS URL от ngrok)
VIDEO_WEBAPP_URL=https://abc123.ngrok-free.app/upload
```

### 6. Перезапустите backend

Остановите backend (`Ctrl+C`) и запустите снова:

```bash
cd webapp
python server.py
```

### 7. Перезапустите бота

Остановите бота (`Ctrl+C`) и запустите снова:

```bash
python bot.py
```

## ✅ Тестирование

1. **Откройте Telegram бота**
2. **Нажмите `/start`**
3. **Должна появиться кнопка "🎬 Загрузить видео"**
4. **Нажмите на неё** - должна открыться Web App страница
5. **Попробуйте загрузить видео**

## 🔍 Проверка работы

### Проверьте backend напрямую:

Откройте в браузере: `https://abc123.ngrok-free.app/upload`

Должна открыться страница загрузки видео.

### Проверьте health endpoint:

```bash
curl https://abc123.ngrok-free.app/health
```

Должен вернуться JSON:

```json
{ "status": "ok", "videos_dir": "..." }
```

## ⚠️ Важные замечания

### ngrok Free план имеет ограничения:

- **Временный URL** - каждый раз при перезапуске ngrok URL меняется
- **Лимит трафика** - ограниченное количество запросов
- **Предупреждение** - ngrok показывает предупреждение при первом посещении

### Для постоянной работы:

Используйте **платный план ngrok** или разверните на сервере (см. `DEPLOY_FOR_BEGINNERS.md`).

## 🐛 Устранение неполадок

### Web App не открывается

1. **Проверьте, что ngrok запущен** (должен быть активен)
2. **Проверьте URL в `.env`** - должен быть HTTPS (не HTTP)
3. **Проверьте, что backend работает** - откройте `https://ваш-ngrok-url/health`

### Ошибка "Invalid URL"

- Убедитесь, что в `VIDEO_WEBAPP_URL` указан полный URL с `/upload` в конце
- Убедитесь, что используется HTTPS (не HTTP)

### Файлы не загружаются

1. **Проверьте права доступа** к папке `webapp/videos/`
2. **Проверьте логи backend** - там будут ошибки
3. **Проверьте размер файла** - не превышает ли лимит

## 📝 Альтернативы ngrok

### 1. Cloudflare Tunnel (бесплатно, постоянный URL)

```bash
# Установите cloudflared
# Windows: скачайте с https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/

# Запустите туннель
cloudflared tunnel --url http://localhost:8000
```

### 2. localtunnel (бесплатно, но менее стабильно)

```bash
npm install -g localtunnel
lt --port 8000
```

### 3. Serveo (SSH туннель, бесплатно)

```bash
ssh -R 80:localhost:8000 serveo.net
```

## 🚀 Следующий шаг

После успешного локального тестирования разверните на сервере для постоянной работы:

📖 **См. `DEPLOY_FOR_BEGINNERS.md`** - полная инструкция по развертыванию на сервере.
