# 📁 Настройка папок upload и converted на сайте

Теперь на сайте доступны две папки:

- **`/upload`** (или `/files`) - загруженные через Web App видео
- **`/converted`** - сконвертированные ботом видео

## 🔧 Настройка на сервере

### 1. Обновите `.env` файл webapp

Добавьте в файл `webapp/.env`:

```env
# Существующие настройки
PUBLIC_BASE_URL=https://ваш-домен.com
VIDEOS_DIR=videos
WEBAPP_PORT=8000
MAX_FILE_SIZE=2147483648

# Новая настройка для папки сконвертированных видео
CONVERTED_DIR=converted
```

**Важно:** `CONVERTED_DIR` должен быть относительным путем от корня проекта `WebConvert` (например, `converted` или `webapp/converted`).

### 2. Обновите `.env` файл бота

Добавьте в файл `.env` в корне проекта:

```env
# Существующие настройки
TELEGRAM_BOT_TOKEN=ваш_токен
OPENAI_API_KEY=ваш_ключ
...

# Новая настройка для копирования сконвертированных видео в веб-папку
WEBAPP_CONVERTED_DIR=webapp/converted
```

**Важно:** `WEBAPP_CONVERTED_DIR` должен быть относительным путем от корня проекта `WebConvert`.

### 3. Обновите конфигурацию Nginx

Добавьте секцию для раздачи файлов из папки `converted`:

```bash
sudo nano /etc/nginx/sites-available/video-upload
```

Добавьте после секции `/videos/`:

```nginx
    # Раздача сконвертированных видео файлов
    location /converted/ {
        alias /root/WebConvert/webapp/converted/;

        # ВАЖНО: Отключаем проверку индексов
        autoindex off;

        # Заголовки для видео
        add_header Content-Type video/mp4;
        add_header Accept-Ranges bytes;

        # Кэширование
        expires 30d;
        add_header Cache-Control "public, immutable";

        # CORS
        add_header Access-Control-Allow-Origin *;

        # Разрешаем доступ к файлам
        access_log off;
    }
```

**Важно:** Замените `/root/WebConvert/webapp/converted/` на правильный путь, если используете другого пользователя.

### 4. Проверьте и перезапустите Nginx

```bash
# Проверьте синтаксис
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### 5. Создайте папку converted

```bash
# Создайте папку для сконвертированных видео
mkdir -p /root/WebConvert/webapp/converted
chmod 755 /root/WebConvert/webapp/converted
```

### 6. Перезапустите сервисы

```bash
# Перезапустите webapp
sudo systemctl restart webapp

# Перезапустите бота
sudo systemctl restart WebConvert
```

## ✅ Проверка

1. **Проверьте доступность страниц:**

   ```bash
   # Загруженные видео
   curl https://ваш-домен.com/files

   # Сконвертированные видео
   curl https://ваш-домен.com/converted
   ```

2. **Проверьте через Web App:**

   - Откройте Web App через кнопку "🎬 Загрузить видео" в боте
   - Должны быть кнопки:
     - "📁 Все файлы" - загруженные видео
     - "🎬 Сконвертированные" - сконвертированные видео

3. **Протестируйте конвертацию:**
   - Отправьте боту видео для конвертации
   - После конвертации файл должен появиться в папке `webapp/converted/`
   - Откройте страницу `/converted` - файл должен быть в списке

## 📝 Структура папок

```
WebConvert/
├── bot.py
├── converted/              # Временная папка для конвертации (удаляется после отправки)
├── downloads/              # Временная папка для скачанных файлов
└── webapp/
    ├── server.py
    ├── videos/             # Загруженные через Web App видео
    │   └── *.mp4
    └── converted/          # Сконвертированные ботом видео (веб-доступ)
        └── *_converted.mp4
```

## 🔍 Доступные страницы

- **`/upload`** - форма загрузки видео
- **`/files`** - список всех загруженных видео
- **`/converted`** - список всех сконвертированных видео
- **`/videos/{filename}`** - прямая ссылка на загруженное видео
- **`/converted/{filename}`** - прямая ссылка на сконвертированное видео

## 🐛 Устранение неполадок

### Файлы не появляются в /converted

1. **Проверьте права доступа:**

   ```bash
   ls -la /root/WebConvert/webapp/converted/
   chmod 755 /root/WebConvert/webapp/converted/
   ```

2. **Проверьте, что бот копирует файлы:**

   ```bash
   # Проверьте логи бота
   sudo journalctl -u WebConvert -n 50 | grep -i converted
   ```

3. **Проверьте конфигурацию Nginx:**
   ```bash
   sudo nginx -t
   sudo tail -f /var/log/nginx/error.log
   ```

### 403 Forbidden для /converted/

См. инструкцию `FIX_403_VIDEOS.md` - примените те же шаги для папки `converted/`.
