# 🚀 Быстрый старт Web App для загрузки видео

## 📋 Что было добавлено

1. **Кнопка "🎬 Загрузить видео"** в главном меню бота (появляется автоматически, если настроен `VIDEO_WEBAPP_URL`)
2. **Backend сервер** (`webapp/server.py`) для загрузки видео файлов
3. **HTML страница** для загрузки видео с интеграцией Telegram Web App API
4. **Обработчик данных** из Web App в боте

## ⚡ Быстрая настройка (локально для тестирования)

### 1. Установите зависимости для backend

```bash
cd webapp
pip install -r requirements.txt
```

### 2. Создайте директорию для видео

```bash
mkdir videos
```

### 3. Настройте переменные окружения

Создайте файл `.env` в корне проекта (или обновите существующий):

```env
# URL для Web App загрузки видео
# Для локального тестирования используйте ngrok или другой туннель
VIDEO_WEBAPP_URL=https://your-domain.com/upload

# Для backend сервера (webapp/.env или переменные окружения):
PUBLIC_BASE_URL=https://your-domain.com
VIDEOS_DIR=videos
WEBAPP_PORT=8000
MAX_FILE_SIZE=2147483648  # 2GB в байтах
```

### 4. Запустите backend сервер

```bash
cd webapp
python server.py
```

Сервер запустится на `http://localhost:8000`

### 5. Для тестирования через Telegram (требуется HTTPS)

Telegram требует HTTPS для Web App. Используйте один из вариантов:

**Вариант A: ngrok (самый простой для тестирования)**

```bash
# Установите ngrok: https://ngrok.com/
ngrok http 8000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`) и добавьте в `.env`:

```env
VIDEO_WEBAPP_URL=https://abc123.ngrok.io/upload
PUBLIC_BASE_URL=https://abc123.ngrok.io
```

**Вариант B: Развертывание на сервере**

См. подробную инструкцию в `WEBAPP_DEPLOY.md`

### 6. Обновите `.env` бота и перезапустите

```env
VIDEO_WEBAPP_URL=https://your-domain.com/upload
```

Перезапустите бота.

## ✅ Проверка работы

1. Откройте бота в Telegram
2. Нажмите кнопку "🎬 Загрузить видео"
3. Должна открыться Web App страница
4. Выберите видео файл (можно больше 50MB)
5. Нажмите "Загрузить видео"
6. После загрузки нажмите "Отправить ссылку в Telegram"
7. Бот отправит вам сообщение с прямой ссылкой на видео

## 📁 Структура файлов

```
webapp/
├── server.py          # FastAPI backend сервер
├── requirements.txt   # Зависимости для backend
├── templates/         # (не используется, HTML встроен в server.py)
└── videos/           # Директория для сохранения видео (создается автоматически)
```

## 🔧 Основные изменения в bot.py

1. **Добавлен импорт:**

   ```python
   from telegram import WebAppInfo
   ```

2. **Добавлена переменная окружения:**

   ```python
   VIDEO_WEBAPP_URL = os.getenv('VIDEO_WEBAPP_URL')
   ```

3. **Добавлена кнопка WebApp в меню:**

   - В функции `start()`
   - В функции `reset_mode()`
   - В функции `button_handler()` (back_to_main)

4. **Добавлен обработчик:**

   ```python
   async def handle_webapp_data(update, context)
   ```

5. **Зарегистрирован обработчик:**
   ```python
   application.add_handler(MessageHandler(filters.StatusUpdate.WEB_APP_DATA, handle_webapp_data))
   ```

## 🐛 Устранение неполадок

### Кнопка "Загрузить видео" не появляется

- Проверьте, что `VIDEO_WEBAPP_URL` установлен в `.env`
- Перезапустите бота

### Web App не открывается

- Убедитесь, что используется HTTPS (не HTTP)
- Проверьте, что URL доступен из интернета
- Проверьте логи бота на наличие ошибок

### Файлы не загружаются

- Проверьте, что backend сервер запущен
- Проверьте права доступа к директории `videos/`
- Проверьте логи backend сервера

### Ошибка "Неверный формат данных"

- Убедитесь, что backend возвращает JSON с полем `video_url`
- Проверьте логи backend и бота

## 📚 Подробная документация

Для развертывания на продакшн сервере см. `WEBAPP_DEPLOY.md`
