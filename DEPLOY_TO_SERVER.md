# 🚀 Подробная инструкция: Загрузка на сервер и подключение домена

Эта инструкция проведет вас через весь процесс: от загрузки репозитория до запуска бота и Web App с вашим доменом.

**Структура проекта:**

- Папка с ботом: `WebConvert`
- Папка `webapp` находится внутри `WebConvert` (т.е. `WebConvert/webapp`)
- Все файлы бота в корне `WebConvert`

## 📋 Что мы будем делать

1. ✅ Подготовка репозитория (Git)
2. 🔌 Подключение к серверу
3. 📥 Загрузка репозитория на сервер
4. ⚙️ Настройка бота на сервере
5. 🎬 Настройка Web App backend
6. 🌐 Настройка Nginx и подключение домена
7. 🔒 Настройка SSL (HTTPS)
8. 🔄 Настройка автозапуска
9. ✅ Проверка работы

---

## 📦 Шаг 1: Подготовка репозитория (если еще нет)

### 1.1. Инициализируйте Git репозиторий (если еще нет)

На вашем компьютере, в папке проекта:

```bash
# Инициализируйте репозиторий
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"
```

### 1.2. Создайте репозиторий на GitHub/GitLab

1. Зайдите на https://github.com (или GitLab)
2. Создайте новый репозиторий
3. **НЕ** инициализируйте его (не добавляйте README, .gitignore и т.д.)
4. Скопируйте URL репозитория (например: `https://github.com/ваш-username/telegram-bot.git`)

### 1.3. Загрузите код на GitHub

```bash
# Добавьте remote
git remote add origin https://github.com/ваш-username/telegram-bot.git

# Загрузите код
git branch -M main
git push -u origin main
```

### 1.4. Создайте файл `.gitignore` (если еще нет)

Создайте файл `.gitignore` в корне проекта:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Environment variables
.env
.env.local

# Downloads and converted files
downloads/
converted/
videos/

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

**Важно:** Не коммитьте `.env` файлы с токенами!

---

## 🔌 Шаг 2: Подключение к серверу

### 2.1. Узнайте IP адрес сервера

В панели управления вашего VPS провайдера найдите IP адрес сервера.

Или на сервере выполните:

```bash
curl ifconfig.me
```

**Запишите IP адрес!** (например: `157.230.123.45`)

### 2.2. Подключитесь к серверу

**На Windows (PowerShell или Git Bash):**

```bash
ssh root@ВАШ_IP_АДРЕС
```

Или если создали пользователя:

```bash
ssh botuser@ВАШ_IP_АДРЕС
```

**При первом подключении:**

- Вас спросят: "Are you sure you want to continue connecting (yes/no/[fingerprint])?"
- Введите: `yes`
- Введите пароль (если используете пароль вместо SSH ключа)

### 2.3. Если не можете подключиться

**Проверьте:**

1. Правильность IP адреса
2. Что сервер запущен
3. Что порт 22 открыт в файрволе
4. Правильность имени пользователя (root или botuser)

---

## 📥 Шаг 3: Загрузка репозитория на сервер

### 3.1. Обновите систему на сервере

```bash
sudo apt update
sudo apt upgrade -y
```

### 3.2. Установите Git (если еще не установлен)

```bash
sudo apt install -y git
```

### 3.3. Создайте директорию для проекта

```bash
# Создаем директорию для бота
mkdir -p ~/WebConvert
cd ~/WebConvert
```

### 3.4. Клонируйте репозиторий

```bash
cd ~/WebConvert
git clone https://github.com/ваш-username/telegram-bot.git .
```

**Замените URL на ваш реальный URL репозитория!**

Если репозиторий приватный, вам нужно будет ввести логин и пароль (или использовать SSH ключ).

### 3.5. Проверьте, что файлы загружены

```bash
ls -la ~/WebConvert/
```

Должны быть файлы: `bot.py`, `video_converter.py`, `text_generator.py`, `requirements.txt`, папка `webapp/` и т.д.

**Проверьте структуру:**

```bash
ls -la ~/WebConvert/webapp/
```

Должны быть файлы: `server.py`, `requirements.txt` и т.д.

---

## ⚙️ Шаг 4: Настройка бота на сервере

### 4.1. Создайте виртуальное окружение

```bash
cd ~/WebConvert
python3 -m venv venv
source venv/bin/activate
```

### 4.2. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4.3. Создайте файл `.env`

```bash
cd ~/WebConvert
nano .env
```

Добавьте следующее (замените на ваши реальные значения):

```env
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# API ключ OpenAI
OPENAI_API_KEY=ваш_openai_ключ

# ID ассистентов GPT (опционально)
GPT_ASSISTANT_ID=asst_xxxxx
GPT_ASSISTANT_ID_VIDEOS=asst_xxxxx

# Локальный Bot API (если используете)
TELEGRAM_LOCAL_API_URL=http://localhost:8081

# URL для Web App (ВАШ ДОМЕН!)
VIDEO_WEBAPP_URL=https://ваш-домен.com/upload
```

**Важно:**

- Замените `ваш-домен.com` на ваш реальный домен!
- Не используйте `http://`, только `https://`!
- URL должен заканчиваться на `/upload`

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 4.4. Создайте необходимые директории

```bash
mkdir -p downloads converted
```

### 4.5. Протестируйте запуск бота

```bash
cd ~/WebConvert
source venv/bin/activate
python bot.py
```

Если всё работает, остановите: `Ctrl+C`

---

## 🎬 Шаг 5: Настройка Web App backend

**Важно:** Папка `webapp` уже находится внутри `WebConvert`, поэтому отдельно копировать не нужно!

### 5.1. Проверьте структуру

```bash
ls -la ~/WebConvert/webapp/
```

Должны быть файлы: `server.py`, `requirements.txt` и т.д.

### 5.2. Создайте виртуальное окружение для backend

```bash
cd ~/WebConvert/webapp
python3 -m venv venv
source venv/bin/activate
```

### 5.3. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 5.4. Создайте директорию для видео

```bash
cd ~/WebConvert/webapp
mkdir -p videos
chmod 755 videos
```

### 5.5. Создайте файл `.env` для backend

```bash
cd ~/WebConvert/webapp
nano .env
```

Добавьте следующее (замените на ваш домен):

```env
# Публичный URL вашего домена (БЕЗ слеша в конце!)
PUBLIC_BASE_URL=https://ваш-домен.com

# Директория для сохранения видео
VIDEOS_DIR=videos

# Порт для backend сервера
WEBAPP_PORT=8000

# Максимальный размер файла в байтах (2GB)
MAX_FILE_SIZE=2147483648
```

**Важно:**

- Замените `ваш-домен.com` на ваш реальный домен!
- Не используйте `http://`, только `https://`!
- URL должен быть БЕЗ слеша в конце!

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 5.6. Протестируйте запуск backend

```bash
cd ~/WebConvert/webapp
source venv/bin/activate
python server.py
```

Если всё работает, остановите: `Ctrl+C`

---

## 🌐 Шаг 6: Настройка DNS (привязка домена к серверу)

### 6.1. Узнайте IP адрес сервера

На сервере выполните:

```bash
curl ifconfig.me
```

**Запишите IP адрес!** 2a03:6f02::1:255(

### 6.2. Настройте DNS записи

Войдите в панель управления вашего регистратора домена.

#### Для Namecheap:

1. Войдите в аккаунт: https://www.namecheap.com/
2. Domain List → Manage (рядом с вашим доменом)
3. Перейдите в раздел **"Advanced DNS"**
4. Найдите секцию **"Host Records"**

5. **Добавьте A-запись для основного домена:**

   - Type: `A Record`
   - Host: `@` (или оставьте пустым)
   - Value: **IP адрес вашего сервера** (из шага 6.1)
   - TTL: `Automatic` (или 300)
   - Нажмите "Save" (зеленая галочка)

6. **Добавьте A-запись для www:**
   - Type: `A Record`
   - Host: `www`
   - Value: **IP адрес вашего сервера**
   - TTL: `Automatic`
   - Нажмите "Save"

#### Для GoDaddy:

1. Войдите в аккаунт: https://www.godaddy.com/
2. My Products → DNS (рядом с вашим доменом)
3. В разделе "Records" нажмите "Add"

4. **Добавьте A-запись для основного домена:**

   - Type: `A`
   - Name: `@`
   - Value: **IP адрес вашего сервера**
   - TTL: `600`
   - Нажмите "Save"

5. **Добавьте A-запись для www:**
   - Type: `A`
   - Name: `www`
   - Value: **IP адрес вашего сервера**
   - TTL: `600`
   - Нажмите "Save"

#### Для Reg.ru:

1. Войдите в аккаунт: https://www.reg.ru/
2. Мои домены → Управление DNS
3. Добавьте A-запись:

   - Поддомен: `@` (или оставьте пустым)
   - IP-адрес: **IP адрес вашего сервера**
   - TTL: `3600`
   - Нажмите "Добавить"

4. Добавьте A-запись для www:
   - Поддомен: `www`
   - IP-адрес: **IP адрес вашего сервера**
   - TTL: `3600`
   - Нажмите "Добавить"

### 6.3. Проверьте DNS

Подождите 5-30 минут (DNS обновляется не мгновенно), затем проверьте:

1. Откройте https://dnschecker.org/
2. Введите ваш домен
3. Выберите тип записи: `A`
4. Нажмите "Search"
5. Убедитесь, что все серверы показывают ваш IP адрес

**Важно:** Не продолжайте, пока DNS не обновится!

---

## 🌐 Шаг 7: Настройка Nginx

### 7.1. Установите Nginx (если еще не установлен)

```bash
sudo apt install -y nginx
```

### 7.2. Создайте конфигурацию Nginx

```bash
sudo nano /etc/nginx/sites-available/video-upload
```

Добавьте следующее (замените `ваш-домен.com` на ваш домен):

```nginx
server {
    listen 80;
    server_name ikurganskiy.ru www.ikurganskiy.ru;

    # Логи
    access_log /var/log/nginx/video-upload-access.log;
    error_log /var/log/nginx/video-upload-error.log;

    # Максимальный размер загружаемого файла (2GB)
    client_max_body_size 2G;
    client_body_timeout 300s;

    # Проксирование на backend
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты для больших файлов
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Раздача статических видео файлов
    location /videos/ {
        alias /root/WebConvert/webapp/videos/;

        # Заголовки для видео
        add_header Content-Type video/mp4;
        add_header Accept-Ranges bytes;

        # Кэширование
        expires 30d;
        add_header Cache-Control "public, immutable";

        # CORS
        add_header Access-Control-Allow-Origin *;
    }
}
```

**Важно:**

- Замените `ваш-домен.com` на ваш реальный домен в двух местах!
- Если используете другого пользователя (не `root`), замените путь `/root/WebConvert/webapp/videos/` на правильный

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 7.3. Активируйте конфигурацию

```bash
sudo ln -s /etc/nginx/sites-available/video-upload /etc/nginx/sites-enabled/
```

### 7.4. Удалите дефолтную конфигурацию (если есть)

```bash
sudo rm -f /etc/nginx/sites-enabled/default
```

### 7.5. Проверьте конфигурацию

```bash
sudo nginx -t
```

Должно быть: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

Если есть ошибки, исправьте их в конфигурации.

### 7.6. Перезапустите Nginx

```bash
sudo systemctl restart nginx
```

### 7.7. Проверьте статус Nginx

```bash
sudo systemctl status nginx
```

Должно быть `active (running)`

---

## 🔒 Шаг 8: Настройка SSL (HTTPS)

### 8.1. Установите Certbot (если еще не установлен)

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 8.2. Получите SSL сертификат

```bash
sudo certbot --nginx -d ikurganskiy.ru -d www.ikurganskiy.ru
```

**Замените `ваш-домен.com` на ваш реальный домен!**

Следуйте инструкциям:

1. Введите email адрес (для уведомлений о сроке действия сертификата)
2. Согласитесь с условиями (введите `A` и нажмите Enter)
3. Выберите, перенаправлять ли HTTP на HTTPS (рекомендуется: `2` и Enter)

### 8.3. Проверьте сертификат

```bash
sudo certbot certificates
```

Должен быть список ваших сертификатов.

### 8.4. Проверьте автообновление

```bash
sudo certbot renew --dry-run
```

Должно быть сообщение об успешном тестовом обновлении.

### 8.5. Проверьте HTTPS

Откройте в браузере: `https://ваш-домен.com`

Должна открыться страница (пока может быть ошибка, если backend не запущен - это нормально).

---

## 🔄 Шаг 9: Настройка автозапуска (systemd)

### 9.1. Создайте service для бота

```bash
sudo nano /etc/systemd/system/WebConvert.service
```

Добавьте следующее (замените `root` на ваше имя пользователя, если другое):

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/WebConvert
Environment="PATH=/root/WebConvert/venv/bin"
ExecStart=/root/WebConvert/venv/bin/python /root/WebConvert/bot.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Важно:**

- Замените `root` на ваше имя пользователя, если другое
- Если используете другого пользователя, замените `/root/WebConvert` на правильный путь (например, `/home/ваш-пользователь/WebConvert`)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 9.2. Создайте service для Web App backend

```bash
sudo nano /etc/systemd/system/webapp.service
```

Добавьте следующее:

```ini
[Unit]
Description=Video Upload WebApp Backend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/WebConvert/webapp
Environment="PATH=/root/WebConvert/webapp/venv/bin"
ExecStart=/root/WebConvert/webapp/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Важно:**

- Замените `root` на ваше имя пользователя, если другое
- Если используете другого пользователя, замените `/root/WebConvert` на правильный путь

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 9.3. Обновите systemd

```bash
sudo systemctl daemon-reload
```

### 9.4. Включите автозапуск

```bash
sudo systemctl enable WebConvert
sudo systemctl enable webapp
```

### 9.5. Запустите сервисы

```bash
sudo systemctl start WebConvert
sudo systemctl start webapp
```

### 9.6. Проверьте статус

```bash
sudo systemctl status WebConvert
sudo systemctl status webapp
```

Оба должны показывать `active (running)` (зеленым цветом).

Если есть ошибки, проверьте логи (см. раздел "Просмотр логов" ниже).

---

## ✅ Шаг 10: Финальная проверка

### 10.1. Проверьте доступность сайта

Откройте в браузере: `https://ваш-домен.com/upload`

Должна открыться страница загрузки видео.

### 10.2. Проверьте health endpoint

```bash
curl https://ваш-домен.com/health
```

Должен вернуться JSON: `{"status":"ok","videos_dir":"..."}`

### 10.3. Проверьте бота в Telegram

1. Откройте Telegram бота
2. Отправьте `/start`
3. **Должна появиться кнопка "🎬 Загрузить видео"**
4. Нажмите на неё
5. Должна открыться Web App страница загрузки

### 10.4. Протестируйте загрузку видео

1. На странице Web App выберите видео файл
2. Нажмите "Загрузить видео"
3. Дождитесь загрузки (прогресс-бар)
4. Нажмите "Отправить ссылку в Telegram"
5. Бот должен отправить вам сообщение с прямой ссылкой на видео

### 10.5. Протестируйте конвертер

1. Отправьте боту видео (через обычную отправку)
2. Бот должен сконвертировать его в MP4 1920x1080

---

## 📊 Просмотр логов

### Логи бота

```bash
# Последние 50 строк
sudo journalctl -u WebConvert -n 50

# В реальном времени (следить)
sudo journalctl -u WebConvert -f
```

### Логи Web App backend

```bash
# Последние 50 строк
sudo journalctl -u webapp -n 50

# В реальном времени
sudo journalctl -u webapp -f
```

### Логи Nginx

```bash
# Ошибки
sudo tail -f /var/log/nginx/video-upload-error.log

# Доступ
sudo tail -f /var/log/nginx/video-upload-access.log
```

---

## 🔧 Полезные команды

### Перезапуск сервисов

```bash
sudo systemctl restart WebConvert
sudo systemctl restart webapp
sudo systemctl restart nginx
```

### Остановка сервисов

```bash
sudo systemctl stop WebConvert
sudo systemctl stop webapp
```

### Проверка статуса

```bash
sudo systemctl status WebConvert
sudo systemctl status webapp
sudo systemctl status nginx
```

### Проверка портов

```bash
# Проверка, что backend слушает на порту 8000
sudo netstat -tulpn | grep 8000

# Проверка HTTP/HTTPS
sudo netstat -tulpn | grep :80
sudo netstat -tulpn | grep :443
```

---

## 🐛 Устранение неполадок

### Ошибка "Conflict: terminated by other getUpdates request"

Эта ошибка означает, что бот запущен в нескольких экземплярах одновременно.

**Решение:**

1. Остановите все экземпляры:

   ```bash
   sudo systemctl stop WebConvert
   pkill -f "bot.py"
   ```

2. Проверьте, что процессов нет:

   ```bash
   ps aux | grep bot.py
   ```

3. Запустите только через systemd:
   ```bash
   sudo systemctl start WebConvert
   ```

**Важно:** Не запускайте бота вручную (`python bot.py`), если он уже запущен через systemd!

Подробнее см. `FIX_CONFLICT_ERROR.md`

### Бот не запускается

1. **Проверьте логи:**

   ```bash
   sudo journalctl -u WebConvert -n 50
   ```

2. **Проверьте .env файл:**

   ```bash
   cat ~/WebConvert/.env
   ```

   Убедитесь, что все токены указаны правильно.

3. **Проверьте пути в service файле:**
   ```bash
   sudo cat /etc/systemd/system/WebConvert.service
   ```

### Backend не запускается

1. **Проверьте логи:**

   ```bash
   sudo journalctl -u webapp -n 50
   ```

2. **Проверьте .env файл:**

   ```bash
   cat ~/WebConvert/webapp/.env
   ```

3. **Проверьте, что порт 8000 свободен:**
   ```bash
   sudo netstat -tulpn | grep 8000
   ```

### Web App не открывается

1. **Проверьте DNS:**

   - Откройте https://dnschecker.org/
   - Введите ваш домен
   - Убедитесь, что он указывает на ваш IP

2. **Проверьте Nginx:**

   ```bash
   sudo systemctl status nginx
   sudo nginx -t
   ```

3. **Проверьте SSL:**

   ```bash
   sudo certbot certificates
   ```

4. **Проверьте логи Nginx:**
   ```bash
   sudo tail -f /var/log/nginx/video-upload-error.log
   ```

### Ошибка 502 Bad Gateway

Это означает, что Nginx не может подключиться к backend.

1. **Проверьте, что backend запущен:**

   ```bash
   sudo systemctl status webapp
   ```

2. **Проверьте, что backend слушает на порту 8000:**

   ```bash
   sudo netstat -tulpn | grep 8000
   ```

3. **Проверьте конфигурацию Nginx:**
   ```bash
   sudo nginx -t
   ```

### Файлы не загружаются

1. **Проверьте права доступа:**

   ```bash
   ls -la ~/WebConvert/webapp/videos/
   ```

   Должны быть права `drwxr-xr-x`

2. **Проверьте размер файла:**

   - Убедитесь, что файл не превышает 2GB

3. **Проверьте логи backend:**
   ```bash
   sudo journalctl -u webapp -f
   ```

---

## 🔄 Обновление кода

Когда вы обновите код в репозитории:

### 1. На сервере обновите код

```bash
cd ~/WebConvert
git pull origin main
```

### 2. Если обновились зависимости

```bash
# Для бота
cd ~/WebConvert
source venv/bin/activate
pip install -r requirements.txt

# Для backend
cd ~/WebConvert/webapp
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Перезапустите сервисы

```bash
sudo systemctl restart WebConvert
sudo systemctl restart webapp
```

---

## 🎉 Готово!

Теперь ваш бот работает на сервере 24/7 с вашим доменом!

**Что дальше:**

- Настройте резервное копирование
- Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade`
- Следите за логами для выявления проблем

**Нужна помощь?** Проверьте логи и убедитесь, что все сервисы запущены.
