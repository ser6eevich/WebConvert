# ⚡ Оптимизация скорости конвертации видео

Инструкция по ускорению конвертации видео с помощью аппаратного ускорения и оптимизированных настроек.

## 🚀 Быстрый старт

### 1. Настройте переменные окружения в `.env`

Добавьте следующие строки в файл `.env` (в корне проекта):

```env
# Настройки скорости конвертации
FFMPEG_PRESET=veryfast
FFMPEG_HWACCEL=auto
```

## 📊 Варианты настройки

### FFMPEG_PRESET (скорость программного кодирования)

Доступные значения (от самого быстрого к самому медленному):

- `ultrafast` - ⚡⚡⚡ Самый быстрый, но самый большой размер файла
- `veryfast` - ⚡⚡ Быстро (рекомендуется, баланс скорости и качества)
- `faster` - ⚡ Быстро
- `fast` - Быстро
- `medium` - Средняя скорость (по умолчанию в FFmpeg)
- `slow` - Медленно, но лучше качество
- `slower` - Очень медленно
- `veryslow` - Самый медленный, но лучшее качество

**Рекомендация:** Используйте `veryfast` для баланса скорости и качества.

### FFMPEG_HWACCEL (аппаратное ускорение)

Доступные значения:

- `auto` - 🔍 Автоматически определяет доступное ускорение (рекомендуется)
- `nvenc` - 🎮 NVIDIA GPU (NVIDIA GeForce, Quadro, Tesla)
- `vaapi` - 🖥️ Intel/AMD GPU (Linux с поддержкой VAAPI)
- `videotoolbox` - 🍎 Apple VideoToolbox (macOS)
- `none` - ❌ Отключить аппаратное ускорение, использовать только CPU

**Рекомендация:** Используйте `auto` - система сама определит лучшее ускорение.

## 🎮 Аппаратное ускорение (GPU)

### NVIDIA NVENC (для серверов/ПК с NVIDIA GPU)

**Преимущества:**

- ⚡ В 3-5 раз быстрее программного кодирования
- 💪 Не нагружает CPU
- ✅ Отличное качество

**Требования:**

- NVIDIA GPU с поддержкой NVENC (GeForce 600+, Quadro K+, Tesla K+)
- Драйверы NVIDIA установлены
- FFmpeg собран с поддержкой NVENC

**Проверка поддержки:**

```bash
ffmpeg -encoders | grep nvenc
```

Если видите `h264_nvenc` или `hevc_nvenc` - поддержка есть!

**Настройка в `.env`:**

```env
FFMPEG_HWACCEL=nvenc
```

### Intel/AMD VAAPI (для Linux серверов)

**Преимущества:**

- ⚡ Быстрее программного кодирования
- 💪 Меньше нагрузка на CPU
- ✅ Хорошее качество

**Требования:**

- Intel/AMD GPU с поддержкой VAAPI
- Установлены драйверы и библиотеки VAAPI

**Проверка поддержки:**

```bash
ffmpeg -encoders | grep vaapi
```

**Настройка в `.env`:**

```env
FFMPEG_HWACCEL=vaapi
```

### Apple VideoToolbox (для macOS)

**Преимущества:**

- ⚡ Быстрое кодирование на Mac
- 💪 Энергоэффективно
- ✅ Хорошее качество

**Требования:**

- macOS 10.13+ (High Sierra или новее)
- FFmpeg с поддержкой VideoToolbox

**Настройка в `.env`:**

```env
FFMPEG_HWACCEL=videotoolbox
```

## 📈 Сравнение скорости

### Примеры времени конвертации (видео 1 час, 1080p):

| Метод                  | Время      | Ускорение |
| ---------------------- | ---------- | --------- |
| Программное (medium)   | ~30-40 мин | 1x        |
| Программное (veryfast) | ~10-15 мин | 2-3x      |
| NVIDIA NVENC           | ~5-8 мин   | 4-6x      |
| Intel VAAPI            | ~8-12 мин  | 3-4x      |
| Apple VideoToolbox     | ~6-10 мин  | 3-5x      |

_Время зависит от мощности процессора/GPU и сложности видео_

## 🔧 Дополнительные оптимизации

### 1. Используйте SSD для временных файлов

Если возможно, разместите папки `downloads/` и `converted/` на SSD для ускорения чтения/записи.

### 2. Увеличьте приоритет процесса (Linux)

```bash
# В systemd service файле добавьте:
Nice=-10
```

### 3. Ограничьте параллельные конвертации

Если конвертируете несколько видео одновременно, это может замедлить процесс. Обрабатывайте по одному файлу.

## 🐛 Устранение неполадок

### Аппаратное ускорение не работает

1. **Проверьте поддержку:**

   ```bash
   ffmpeg -encoders | grep -E "nvenc|vaapi|videotoolbox"
   ```

2. **Проверьте драйверы:**

   ```bash
   # Для NVIDIA
   nvidia-smi

   # Для Intel/AMD (Linux)
   vainfo
   ```

3. **Установите FFmpeg с поддержкой ускорения:**

   ```bash
   # Ubuntu/Debian
   sudo apt install ffmpeg

   # Или соберите из исходников с нужными флагами
   ```

### Конвертация все еще медленная

1. **Проверьте текущие настройки:**

   - Убедитесь, что `FFMPEG_PRESET=veryfast` или `ultrafast`
   - Проверьте, что `FFMPEG_HWACCEL=auto` или конкретный тип

2. **Проверьте нагрузку системы:**

   ```bash
   htop  # или top
   ```

3. **Проверьте логи бота:**
   ```bash
   sudo journalctl -u WebConvert -f
   ```
   Должны увидеть сообщения о типе используемого ускорения.

## 📝 Примеры конфигураций

### Максимальная скорость (для тестирования)

```env
FFMPEG_PRESET=ultrafast
FFMPEG_HWACCEL=auto
```

### Баланс скорости и качества (рекомендуется)

```env
FFMPEG_PRESET=veryfast
FFMPEG_HWACCEL=auto
```

### Лучшее качество (медленнее)

```env
FFMPEG_PRESET=medium
FFMPEG_HWACCEL=auto
```

### Только программное кодирование (если GPU недоступен)

```env
FFMPEG_PRESET=veryfast
FFMPEG_HWACCEL=none
```

## ✅ После настройки

1. **Перезапустите бота:**

   ```bash
   sudo systemctl restart WebConvert
   ```

2. **Проверьте логи:**

   ```bash
   sudo journalctl -u WebConvert -f
   ```

   При конвертации должны увидеть сообщения о типе используемого ускорения.

3. **Протестируйте:**
   - Отправьте тестовое видео в бота
   - Следите за логами - должно быть указано используемое ускорение
   - Сравните время конвертации с предыдущими результатами

## 🎯 Итоговые рекомендации

1. **Для большинства случаев:** `FFMPEG_PRESET=veryfast` + `FFMPEG_HWACCEL=auto`
2. **Если есть NVIDIA GPU:** `FFMPEG_HWACCEL=nvenc` (самое быстрое)
3. **Если нет GPU:** `FFMPEG_PRESET=veryfast` + `FFMPEG_HWACCEL=none`
4. **Для максимальной скорости:** `FFMPEG_PRESET=ultrafast` (но файлы будут больше)

Удачи! 🚀
