# 🚀 Полная инструкция по развертыванию для начинающих

Эта инструкция поможет вам шаг за шагом развернуть бота и Web App на сервере, даже если вы никогда этого не делали.

## 📋 Содержание

1. [Что вам понадобится](#что-вам-понадобится)
2. [Шаг 1: Аренда сервера (VPS)](#шаг-1-аренда-сервера-vps)
3. [Шаг 2: Получение домена](#шаг-2-получение-домена)
4. [Шаг 3: Подключение к серверу](#шаг-3-подключение-к-серверу)
5. [Шаг 4: Настройка сервера](#шаг-4-настройка-сервера)
6. [Шаг 5: Установка и настройка бота](#шаг-5-установка-и-настройка-бота)
7. [Шаг 6: Установка и настройка Web App backend](#шаг-6-установка-и-настройка-web-app-backend)
8. [Шаг 7: Настройка Nginx и SSL](#шаг-7-настройка-nginx-и-ssl)
9. [Шаг 8: Проверка работы](#шаг-8-проверка-работы)
10. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## 🛒 Что вам понадобится

1. **Сервер (VPS)** - виртуальный сервер в интернете
2. **Домен** - адрес вашего сайта (например: `mybot.com`)
3. **Базовые знания** - умение пользоваться командной строкой (мы всё объясним!)

**Примерная стоимость:**

- VPS: от $5-10/месяц
- Домен: от $10-15/год
- **Итого: ~$70-135 в первый год**

---

## 🖥️ Шаг 1: Аренда сервера (VPS)

VPS (Virtual Private Server) - это виртуальный компьютер в интернете, который работает 24/7.

### Рекомендуемые провайдеры:

1. **DigitalOcean** (рекомендуется для начинающих)

   - Сайт: https://www.digitalocean.com/
   - Минимальный тариф: $6/месяц (1GB RAM, 1 CPU)
   - Простой интерфейс, хорошая документация

2. **Hetzner** (дешевле, но сложнее)

   - Сайт: https://www.hetzner.com/
   - Минимальный тариф: ~€4/месяц
   - Немецкий провайдер, хорошее качество

3. **Vultr**

   - Сайт: https://www.vultr.com/
   - Минимальный тариф: $6/месяц
   - Хорошая производительность

4. **Timeweb** (российский, для русскоязычных)
   - Сайт: https://timeweb.com/
   - Минимальный тариф: от 200₽/месяц
   - Русскоязычная поддержка

### Как зарегистрироваться и создать сервер (на примере DigitalOcean):

1. **Зарегистрируйтесь:**

   - Перейдите на https://www.digitalocean.com/
   - Нажмите "Sign Up"
   - Заполните форму (можно использовать GitHub аккаунт)

2. **Создайте Droplet (сервер):**

   - Нажмите "Create" → "Droplets"
   - **Выберите регион:** ближайший к вам (например, Frankfurt для Европы)
   - **Выберите образ:** Ubuntu 22.04 (LTS) x64
   - **Выберите план:** Basic, $6/месяц (1GB RAM, 1 CPU, 25GB SSD)
   - **Добавьте SSH ключ** (см. ниже) или выберите "Password" для авторизации
   - **Имя сервера:** любое (например: `my-telegram-bot`)
   - Нажмите "Create Droplet"

3. **Дождитесь создания сервера** (1-2 минуты)

4. **Запишите IP адрес сервера:**
   - После создания вы увидите IP адрес (например: `157.230.123.45`)
   - **Сохраните его!** Он понадобится для подключения

### Создание SSH ключа (для безопасности):

**На Windows:**

1. Откройте PowerShell или Git Bash
2. Выполните:
   ```bash
   ssh-keygen -t rsa -b 4096
   ```
3. Нажмите Enter 3 раза (используйте значения по умолчанию)
4. Найдите файл `C:\Users\ВашеИмя\.ssh\id_rsa.pub`
5. Откройте его в блокноте и скопируйте содержимое
6. В DigitalOcean: Settings → Security → SSH Keys → Add SSH Key
7. Вставьте скопированный ключ

**На Linux/Mac:**

```bash
ssh-keygen -t rsa -b 4096
cat ~/.ssh/id_rsa.pub
# Скопируйте вывод и добавьте в DigitalOcean
```

---

## 🌐 Шаг 2: Получение домена

Домен - это адрес вашего сайта (например: `mybot.com` вместо IP адреса).

### Рекомендуемые регистраторы доменов:

1. **Namecheap** (рекомендуется)

   - Сайт: https://www.namecheap.com/
   - Простой интерфейс, хорошие цены
   - Поддержка .com, .net, .org и других

2. **GoDaddy**

   - Сайт: https://www.godaddy.com/
   - Популярный, но дороже

3. **Reg.ru** (для русскоязычных)
   - Сайт: https://www.reg.ru/
   - Русскоязычная поддержка
   - Поддержка .ru, .рф доменов

### Как купить домен (на примере Namecheap):

1. **Зарегистрируйтесь:**

   - Перейдите на https://www.namecheap.com/
   - Нажмите "Sign Up"
   - Заполните форму

2. **Найдите свободный домен:**

   - Введите желаемое имя (например: `mytelegrambot`)
   - Выберите расширение (`.com`, `.net`, `.org` и т.д.)
   - Нажмите "Search"
   - Если домен свободен, нажмите "Add to Cart"

3. **Оформите заказ:**

   - Добавьте домен в корзину
   - Отключите ненужные услуги (WhoisGuard можно оставить)
   - Оплатите (обычно $10-15/год)

4. **Запишите домен:**
   - Например: `mytelegrambot.com`
   - **Сохраните его!**

### Настройка DNS (привязка домена к серверу):

После покупки домена нужно "привязать" его к вашему серверу.

1. **Войдите в панель управления доменом** (Namecheap → Domain List → Manage)

2. **Найдите раздел "Advanced DNS" или "DNS"**

3. **Добавьте A-запись:**

   - Type: `A Record`
   - Host: `@` (или оставьте пустым)
   - Value: **IP адрес вашего сервера** (из Шага 1)
   - TTL: `Automatic` (или 300)
   - Нажмите "Save"

4. **Добавьте A-запись для www:**

   - Type: `A Record`
   - Host: `www`
   - Value: **IP адрес вашего сервера**
   - TTL: `Automatic`
   - Нажмите "Save"

5. **Подождите 5-30 минут** (DNS обновляется не мгновенно)

6. **Проверьте:**
   - Откройте https://dnschecker.org/
   - Введите ваш домен
   - Убедитесь, что он указывает на ваш IP

---

## 🔌 Шаг 3: Подключение к серверу

Теперь нужно подключиться к серверу через SSH (Secure Shell).

### На Windows:

1. **Установите PuTTY** (если нет):

   - Скачайте: https://www.putty.org/
   - Или используйте Windows Terminal (Windows 10/11)

2. **Подключитесь:**

   - Откройте PowerShell или Command Prompt
   - Выполните:
     ```bash
     ssh root@ВАШ_IP_АДРЕС
     ```
   - Например: `ssh root@157.230.123.45`
   - При первом подключении нажмите "yes" на вопрос о подтверждении

3. **Введите пароль** (если использовали пароль вместо SSH ключа)

### На Linux/Mac:

```bash
ssh root@ВАШ_IP_АДРЕС
```

**Готово!** Вы подключены к серверу. Теперь все команды выполняются на сервере.

---

## ⚙️ Шаг 4: Настройка сервера

### 1. Обновите систему:

```bash
apt update
apt upgrade -y
```

### 2. Создайте нового пользователя (для безопасности):

```bash
# Создаем пользователя
adduser botuser

# Добавляем в группу sudo (для выполнения команд с правами администратора)
usermod -aG sudo botuser

# Переключаемся на нового пользователя
su - botuser
```

**Примечание:** В дальнейшем используйте `botuser` вместо `root`.

### 3. Настройте файрвол (UFW):

```bash
# Устанавливаем UFW
sudo apt install -y ufw

# Разрешаем SSH (чтобы не потерять доступ)
sudo ufw allow 22/tcp

# Разрешаем HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включаем файрвол
sudo ufw enable

# Проверяем статус
sudo ufw status
```

### 4. Установите необходимые пакеты:

```bash
sudo apt install -y python3 python3-pip python3-venv nginx certbot python3-certbot-nginx git
```

---

## 🤖 Шаг 5: Установка и настройка бота

### 1. Создайте директорию для бота:

```bash
cd ~
mkdir telegram-bot
cd telegram-bot
```

### 2. Загрузите файлы бота на сервер:

**Вариант A: Через Git (если проект в репозитории):**

```bash
git clone https://github.com/ваш-username/ваш-репозиторий.git .
```

**Вариант B: Через SCP (с вашего компьютера):**

На вашем компьютере (в PowerShell или терминале):

```bash
scp -r D:\coding\webconvert\* botuser@ВАШ_IP:/home/botuser/telegram-bot/
```

**Вариант C: Вручную через SFTP:**

- Используйте FileZilla или WinSCP
- Подключитесь к серверу
- Скопируйте все файлы проекта

### 3. Создайте виртуальное окружение:

```bash
cd ~/telegram-bot
python3 -m venv venv
source venv/bin/activate
```

### 4. Установите зависимости:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 5. Создайте файл `.env`:

```bash
nano .env
```

Добавьте следующее (замените на ваши значения):

```env
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# API ключ OpenAI
OPENAI_API_KEY=ваш_openai_ключ

# ID ассистентов GPT (опционально)
GPT_ASSISTANT_ID=asst_xxxxx
GPT_ASSISTANT_ID_VIDEOS=asst_xxxxx

# Локальный Bot API (если используете)
TELEGRAM_LOCAL_API_URL=http://localhost:8081

# URL для Web App (пока оставьте пустым, настроим позже)
VIDEO_WEBAPP_URL=https://ваш-домен.com/upload
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 6. Протестируйте запуск бота:

```bash
python bot.py
```

Если всё работает, остановите бота: `Ctrl+C`

---

## 🎬 Шаг 6: Установка и настройка Web App backend

### 1. Создайте директорию для Web App:

```bash
cd ~
mkdir video-upload-webapp
cd video-upload-webapp
```

### 2. Скопируйте папку `webapp`:

```bash
# Если файлы уже на сервере
cp -r ~/telegram-bot/webapp ~/video-upload-webapp/

# Или скопируйте через SCP с вашего компьютера
```

### 3. Создайте виртуальное окружение:

```bash
cd ~/video-upload-webapp
python3 -m venv venv
source venv/bin/activate
```

### 4. Установите зависимости:

```bash
cd webapp
pip install --upgrade pip
pip install -r requirements.txt
```

### 5. Создайте директорию для видео:

```bash
cd ~/video-upload-webapp
mkdir videos
chmod 755 videos
```

### 6. Создайте файл `.env`:

```bash
cd ~/video-upload-webapp
nano .env
```

Добавьте:

```env
# Публичный URL вашего домена (БЕЗ слеша в конце!)
PUBLIC_BASE_URL=https://ваш-домен.com

# Директория для сохранения видео
VIDEOS_DIR=videos

# Порт для backend сервера
WEBAPP_PORT=8000

# Максимальный размер файла в байтах (2GB)
MAX_FILE_SIZE=2147483648
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 7. Протестируйте запуск backend:

```bash
cd ~/video-upload-webapp/webapp
source ~/video-upload-webapp/venv/bin/activate
python server.py
```

Если всё работает, остановите: `Ctrl+C`

---

## 🌐 Шаг 7: Настройка Nginx и SSL

### 1. Создайте конфигурацию Nginx:

```bash
sudo nano /etc/nginx/sites-available/video-upload
```

Добавьте следующее (замените `ваш-домен.com` на ваш домен):

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Логи
    access_log /var/log/nginx/video-upload-access.log;
    error_log /var/log/nginx/video-upload-error.log;

    # Максимальный размер загружаемого файла (2GB)
    client_max_body_size 2G;
    client_body_timeout 300s;

    # Проксирование на backend
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты для больших файлов
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Раздача статических видео файлов
    location /videos/ {
        alias /home/botuser/video-upload-webapp/videos/;

        # Заголовки для видео
        add_header Content-Type video/mp4;
        add_header Accept-Ranges bytes;

        # Кэширование
        expires 30d;
        add_header Cache-Control "public, immutable";

        # CORS
        add_header Access-Control-Allow-Origin *;
    }
}
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 2. Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/video-upload /etc/nginx/sites-enabled/
```

### 3. Удалите дефолтную конфигурацию (если есть):

```bash
sudo rm /etc/nginx/sites-enabled/default
```

### 4. Проверьте конфигурацию:

```bash
sudo nginx -t
```

Должно быть: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

### 5. Перезапустите Nginx:

```bash
sudo systemctl restart nginx
```

### 6. Получите SSL сертификат (HTTPS):

```bash
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

Следуйте инструкциям:

- Введите email
- Согласитесь с условиями
- Выберите, перенаправлять ли HTTP на HTTPS (рекомендуется: 2)

### 7. Проверьте автоматическое обновление сертификата:

```bash
sudo certbot renew --dry-run
```

---

## 🔄 Шаг 8: Автозапуск через systemd

### 1. Создайте service для бота:

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Добавьте:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/telegram-bot
Environment="PATH=/home/botuser/telegram-bot/venv/bin"
ExecStart=/home/botuser/telegram-bot/venv/bin/python /home/botuser/telegram-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 2. Создайте service для Web App backend:

```bash
sudo nano /etc/systemd/system/video-upload-webapp.service
```

Добавьте:

```ini
[Unit]
Description=Video Upload WebApp Backend
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/video-upload-webapp/webapp
Environment="PATH=/home/botuser/video-upload-webapp/venv/bin"
ExecStart=/home/botuser/video-upload-webapp/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 3. Обновите `.env` бота с правильным URL:

```bash
nano ~/telegram-bot/.env
```

Убедитесь, что есть строка:

```env
VIDEO_WEBAPP_URL=https://ваш-домен.com/upload
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 4. Активируйте и запустите сервисы:

```bash
# Бот
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# Web App backend
sudo systemctl enable video-upload-webapp
sudo systemctl start video-upload-webapp
```

### 5. Проверьте статус:

```bash
sudo systemctl status telegram-bot
sudo systemctl status video-upload-webapp
```

Если всё работает, вы увидите `active (running)`

### 6. Просмотр логов:

```bash
# Логи бота
sudo journalctl -u telegram-bot -f

# Логи Web App backend
sudo journalctl -u video-upload-webapp -f
```

---

## ✅ Шаг 9: Проверка работы

### 1. Проверьте бота:

- Откройте Telegram
- Найдите вашего бота
- Отправьте `/start`
- Должна появиться кнопка "🎬 Загрузить видео"

### 2. Проверьте Web App:

- Нажмите кнопку "🎬 Загрузить видео"
- Должна открыться страница загрузки
- Попробуйте загрузить небольшое видео

### 3. Проверьте доступность сайта:

Откройте в браузере: `https://ваш-домен.com/upload`

Должна открыться страница загрузки.

---

## ❓ Часто задаваемые вопросы

### Как перезапустить бота после изменений?

```bash
sudo systemctl restart telegram-bot
```

### Как посмотреть логи бота?

```bash
sudo journalctl -u telegram-bot -f
```

### Как обновить код бота?

```bash
cd ~/telegram-bot
# Обновите файлы (через git pull или scp)
source venv/bin/activate
pip install -r requirements.txt  # Если обновились зависимости
sudo systemctl restart telegram-bot
```

### Как изменить порт backend?

1. Измените `WEBAPP_PORT` в `~/video-upload-webapp/.env`
2. Измените `proxy_pass` в `/etc/nginx/sites-available/video-upload`
3. Перезапустите:
   ```bash
   sudo systemctl restart video-upload-webapp
   sudo systemctl restart nginx
   ```

### Что делать, если бот не запускается?

1. Проверьте логи: `sudo journalctl -u telegram-bot -n 50`
2. Проверьте `.env` файл (правильные ли токены)
3. Проверьте, что виртуальное окружение активировано в service файле

### Как очистить старые видео?

```bash
# Удалить файлы старше 30 дней
find ~/video-upload-webapp/videos -type f -mtime +30 -delete
```

Можно добавить в cron для автоматической очистки.

---

## 🎉 Готово!

Теперь ваш бот работает на сервере 24/7, и пользователи могут загружать видео любого размера через Web App!

**Что дальше:**

- Настройте резервное копирование
- Настройте мониторинг (опционально)
- Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade`

**Нужна помощь?** Проверьте логи и убедитесь, что все сервисы запущены.
