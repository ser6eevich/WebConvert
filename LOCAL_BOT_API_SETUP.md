# Подробная инструкция по настройке локального Telegram Bot API сервера

Эта инструкция поможет вам настроить локальный Bot API сервер для работы с большими видео файлами (до 2GB).

## 📋 Что такое локальный Bot API и зачем он нужен?

**Стандартный Telegram Bot API:**

- ❌ Может скачать файлы только до 20MB
- ❌ Может отправить файлы только до 50MB

**Локальный Bot API сервер:**

- ✅ Может скачать файлы до 2GB
- ✅ Может отправить файлы до 2GB
- ✅ Работает быстрее для больших файлов

**Простыми словами:** Локальный Bot API - это копия серверов Telegram, которая работает на вашем сервере. Она позволяет обойти ограничения стандартного API.

---

## 🖥️ Где что делать: ПК или Сервер?

**Важно понять разницу:**

- **ПК (ваш компьютер)** - здесь вы будете:

  - Получать API credentials
  - Настраивать бота (файл `.env`)
  - Запускать бота (`bot.py`)

- **Сервер** - здесь вы будете:
  - Устанавливать Docker
  - Запускать локальный Bot API сервер
  - Хранить временные файлы

**Схема работы:**

```
ПК (ваш компьютер)          Сервер
     │                          │
     │  ┌─────────────┐         │
     │  │  bot.py     │         │
     │  │  (бот)      │─────────┼──> Локальный Bot API
     │  └─────────────┘         │    (Docker контейнер)
     │                          │
     └──────────────────────────┘
```

---

## 📝 ЧАСТЬ 1: Действия на ПК (вашем компьютере)

### Шаг 1.1: Получение API credentials (api_id и api_hash)

Это нужно для того, чтобы ваш локальный сервер мог подключиться к Telegram.

#### 1.1.1. Откройте сайт Telegram

На вашем ПК откройте браузер и перейдите на: **https://my.telegram.org/**

#### 1.1.2. Войдите в аккаунт

1. Введите ваш номер телефона в формате: `+79991234567` (с плюсом и кодом страны)
2. Нажмите "Next" (Далее)
3. Введите код подтверждения, который придет в Telegram
4. Если попросит пароль - введите пароль от вашего аккаунта Telegram

#### 1.1.3. Перейдите в раздел API

1. После входа вы увидите страницу с информацией о вашем аккаунте
2. Найдите и нажмите на ссылку **"API development tools"** или **"API development"**
3. Если такой ссылки нет, попробуйте напрямую: https://my.telegram.org/apps

#### 1.1.4. Создайте приложение

1. Если вы впервые здесь, нажмите **"Create application"** или **"Create new application"**
2. Заполните форму:
   - **App title** (Название приложения): можете написать что угодно, например: "My Bot API Server"
   - **Short name** (Короткое имя): например: "mybotapi"
   - **Platform** (Платформа): выберите "Desktop" или "Other"
   - **Description** (Описание): можете оставить пустым или написать что-то вроде "Local Bot API Server"
3. Нажмите **"Create"** или **"Submit"**

#### 1.1.5. Скопируйте данные

После создания приложения вы увидите две важные строки:

- **api_id** - это число, например: `12345678`
- **api_hash** - это строка из букв и цифр, например: `abcdef1234567890abcdef1234567890`

⚠️ **ВАЖНО:** Сохраните эти данные в безопасном месте! Они понадобятся для запуска сервера.

**Пример того, что вы увидите:**

```
api_id: 12345678
api_hash: abcdef1234567890abcdef1234567890
```

**Скопируйте оба значения и сохраните их - они понадобятся на сервере!**

---

## 🖥️ ЧАСТЬ 2: Действия на СЕРВЕРЕ

Теперь переходим к настройке на сервере. Подключитесь к серверу по SSH или откройте терминал на сервере.

### Шаг 2.1: Установка Docker на сервере

Docker - это программа, которая позволяет запускать другие программы в изолированных контейнерах.

#### 2.1.1. Подключитесь к серверу

**Если сервер Linux (Ubuntu/Debian):**

1. Откройте терминал на вашем ПК
2. Подключитесь к серверу через SSH:
   ```bash
   ssh username@your-server-ip
   ```
   Например: `ssh root@192.168.1.100`

**Если сервер Windows:**

- Используйте Remote Desktop или PowerShell для удаленного подключения

#### 2.1.2. Установите Docker на Linux сервере

**Для Ubuntu/Debian:**

1. **Обновите список пакетов:**

   ```bash
   sudo apt update
   ```

2. **Установите необходимые пакеты:**

   ```bash
   sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release
   ```

3. **Добавьте ключ Docker:**

   ```bash
   curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
   ```

4. **Добавьте репозиторий Docker:**

   ```bash
   echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
   ```

5. **Обновите список пакетов после добавления репозитория:**

   ```bash
   sudo apt update
   ```

6. **Установите Docker:**

   ```bash
   sudo apt install -y docker-ce docker-ce-cli containerd.io
   ```

   ⚠️ **Если видите ошибку "Package 'docker-ce' has no installation candidate":**

   Это может быть из-за:

   - Неправильной архитектуры (не amd64)
   - Неправильной версии Ubuntu/Debian
   - Проблем с репозиторием

   **Решение 1: Проверьте архитектуру и версию ОС:**

   ```bash
   # Проверьте архитектуру
   uname -m
   # Должно быть: x86_64 или amd64

   # Проверьте версию Ubuntu/Debian
   lsb_release -a
   ```

   **Решение 2: Используйте универсальный скрипт установки Docker:**

   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   ```

   Этот скрипт автоматически определит вашу систему и установит Docker правильно.

   **Решение 3: Если сервер не amd64 (например, ARM):**

   ```bash
   # Для ARM архитектуры используйте другой репозиторий
   echo "deb [arch=arm64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
   sudo apt update
   sudo apt install -y docker-ce docker-ce-cli containerd.io
   ```

7. **Добавьте вашего пользователя в группу docker:**

   ```bash
   sudo usermod -aG docker $USER
   ```

8. **Перезайдите в систему** (или выполните `newgrp docker`)

9. **Проверьте установку:**
   ```bash
   docker --version
   ```
   Должно показать версию, например: `Docker version 24.0.0`

**Для CentOS/RHEL:**

1. **Установите необходимые пакеты:**

   ```bash
   sudo yum install -y yum-utils
   ```

2. **Добавьте репозиторий Docker:**

   ```bash
   sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
   ```

3. **Установите Docker:**

   ```bash
   sudo yum install -y docker-ce docker-ce-cli containerd.io
   ```

4. **Запустите Docker:**

   ```bash
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

5. **Добавьте пользователя в группу docker:**

   ```bash
   sudo usermod -aG docker $USER
   ```

6. **Проверьте установку:**
   ```bash
   docker --version
   ```

---

### Шаг 2.2: Запуск локального Bot API сервера на сервере

Теперь на сервере запустим локальный Bot API сервер.

#### 2.2.1. Откройте терминал на сервере

Убедитесь, что вы подключены к серверу и находитесь в терминале сервера.

#### 2.2.2. Запустите Docker контейнер

**Замените в команде:**

- `your_api_id` - на ваш api_id (число, которое вы получили на ПК в шаге 1.1.5)
- `your_api_hash` - на ваш api_hash (строка, которую вы получили на ПК в шаге 1.1.5)

**Команда для запуска:**

```bash
docker run -d \
  --name telegram-bot-api \
  -p 8081:8081 \
  -v telegram-bot-api-data:/var/lib/telegram-bot-api \
  -e TELEGRAM_API_ID=32312962 \
  -e TELEGRAM_API_HASH=18e1940e6b21230d9cd6338d7a9598d5 \
  --restart unless-stopped \
  aiogram/telegram-bot-api:latest
```

**Пример с реальными данными:**

```bash
docker run -d \
  --name telegram-bot-api \
  -p 8081:8081 \
  -v telegram-bot-api-data:/var/lib/telegram-bot-api \
  -e TELEGRAM_API_ID=12345678 \
  -e TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890 \
  --restart unless-stopped \
  aiogram/telegram-bot-api:latest
```

**Что делает эта команда:**

- `docker run` - запустить контейнер Docker
- `-d` - запустить в фоновом режиме (detached)
- `--name telegram-bot-api` - назвать контейнер "telegram-bot-api"
- `-p 8081:8081` - открыть порт 8081 на сервере
- `-v telegram-bot-api-data:/var/lib/telegram-bot-api` - сохранить данные в постоянное хранилище
- `-e TELEGRAM_API_ID=...` - передать api_id в контейнер
- `-e TELEGRAM_API_HASH=...` - передать api_hash в контейнер
- `--restart unless-stopped` - автоматически перезапускать при перезагрузке сервера
- `aiogram/telegram-bot-api:latest` - образ Docker для Bot API

#### 2.2.3. Проверьте, что контейнер запустился

Выполните на сервере:

```bash
docker ps
```

Вы должны увидеть контейнер с именем `telegram-bot-api` в списке.

Если контейнера нет, проверьте логи на сервере:

```bash
docker logs telegram-bot-api
```

#### 2.2.4. Узнайте IP адрес сервера

Выполните на сервере одну из команд:

**Для Linux:**

```bash
hostname -I
```

или

```bash
ip addr show
```

**Для Windows:**

```powershell
ipconfig
```

Найдите IP адрес (обычно выглядит как `192.168.1.100` или `10.0.0.5`).

**Запишите этот IP адрес - он понадобится на ПК!**
72.56.73.219 172.17.0.1 2a03:6f02::1:255

---

### Шаг 2.3: Проверка работы сервера

#### 2.3.1. Получите токен вашего бота (на ПК)

1. На вашем ПК откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/mybots`
4. Выберите вашего бота
5. Нажмите "API Token" или отправьте `/token`
6. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 2.3.2. Проверьте работу сервера (на сервере или с ПК)

**Если проверяете с сервера:**

```bash
curl http://72.56.73.219:8081/8526449683:AAHAdvfuSYebi_9WP3TK5XAt7w7VkTKwdOk/getMe
```

**Если проверяете с ПК (замените `SERVER_IP` на IP адрес сервера):**

**Windows (PowerShell):**

```powershell
curl http://SERVER_IP:8081/botYOUR_BOT_TOKEN/getMe
```

**Linux/macOS:**

```bash
curl http://SERVER_IP:8081/botYOUR_BOT_TOKEN/getMe
```

**Замените `YOUR_BOT_TOKEN` на реальный токен вашего бота!**

**Пример:**

```bash
curl http://192.168.1.100:8081/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz/getMe
```

#### 2.3.3. Что должно получиться?

Если всё работает правильно, вы увидите JSON ответ примерно такого вида:

```json
{
	"ok": true,
	"result": {
		"id": 123456789,
		"is_bot": true,
		"first_name": "Your Bot Name",
		"username": "your_bot_username"
	}
}
```

Если видите ошибку или ничего не происходит:

- Убедитесь, что Docker контейнер запущен на сервере: `docker ps`
- Проверьте логи на сервере: `docker logs telegram-bot-api`
- Убедитесь, что порт 8081 не занят другой программой на сервере
- Проверьте firewall на сервере (если проверяете с ПК)

---

## 💻 ЧАСТЬ 3: Настройка бота на ПК

Теперь вернемся на ваш ПК и настроим бота для работы с локальным сервером.

### Шаг 3.1: Откройте файл `.env` на ПК

Найдите файл `.env` в папке с вашим ботом (там же, где `bot.py`).

### Шаг 3.2: Добавьте строку в `.env` на ПК

Откройте файл `.env` в любом текстовом редакторе (Блокнот, VS Code и т.д.) и добавьте строку:

**Если сервер на том же компьютере, что и бот:**

```env
TELEGRAM_LOCAL_API_URL=http://localhost:8081
```

**Если сервер на другом компьютере (замените `SERVER_IP` на IP адрес сервера):**

```env
TELEGRAM_LOCAL_API_URL=http://SERVER_IP:8081
```

**Пример:**

```env
TELEGRAM_LOCAL_API_URL=http://192.168.1.100:8081
```

### Шаг 3.3: Сохраните файл на ПК

Сохраните файл `.env` и закройте редактор.

### Шаг 3.4: Перезапустите бота на ПК

1. Остановите бота (если он запущен) - нажмите Ctrl+C в терминале на ПК
2. Запустите бота снова на ПК:
   - **Windows:** `py bot.py`
   - **Linux/macOS:** `python3 bot.py`

### Шаг 3.5: Проверьте логи на ПК

При запуске бота на ПК вы должны увидеть в логах:

```
Используется локальный Bot API: http://SERVER_IP:8081
```

Если видите это сообщение - всё работает правильно! 🎉

---

## 🔧 Полезные команды для управления сервером

Все эти команды выполняются **на сервере**:

### Просмотр логов сервера

```bash
docker logs telegram-bot-api
```

### Просмотр последних 50 строк логов

```bash
docker logs --tail 50 telegram-bot-api
```

### Остановка сервера

```bash
docker stop telegram-bot-api
```

### Запуск остановленного сервера

```bash
docker start telegram-bot-api
```

### Перезапуск сервера

```bash
docker restart telegram-bot-api
```

### Удаление контейнера (если нужно начать заново)

```bash
docker stop telegram-bot-api
docker rm telegram-bot-api
```

После этого можно запустить заново командой из шага 2.2.2.

### Просмотр использования ресурсов

```bash
docker stats telegram-bot-api
```

---

## 🛠️ Решение проблем

### Проблема 1: "Port 8081 is already in use" (на сервере)

**Причина:** Порт 8081 уже занят другой программой на сервере.

**Решение:**

1. На сервере найдите, какая программа использует порт:
   ```bash
   netstat -tuln | grep 8081
   ```
   или
   ```bash
   lsof -i :8081
   ```
2. Остановите эту программу или используйте другой порт (например, 8082):
   ```bash
   docker run -d --name telegram-bot-api -p 8082:8081 ...
   ```
   И в `.env` на ПК укажите: `TELEGRAM_LOCAL_API_URL=http://SERVER_IP:8082`

### Проблема 2: "Package 'docker-ce' has no installation candidate" (на сервере)

**Причина:** Проблемы с репозиторием Docker или неподдерживаемая архитектура.

**Решение:**

1. **Проверьте архитектуру сервера:**

   ```bash
   uname -m
   ```

   Если не `x86_64` или `amd64`, используйте соответствующий репозиторий.

2. **Используйте универсальный скрипт установки (рекомендуется):**

   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   ```

   Этот скрипт автоматически определит вашу систему и установит Docker.

3. **Или установите через snap (альтернатива):**

   ```bash
   sudo snap install docker
   ```

4. **Проверьте версию Ubuntu/Debian:**
   ```bash
   lsb_release -a
   ```
   Убедитесь, что версия поддерживается Docker.

### Проблема 3: "Cannot connect to Docker daemon" (на сервере)

**Причина:** Docker не запущен на сервере.

**Решение:**

- **Linux:** Запустите Docker на сервере: `sudo systemctl start docker`
- Проверьте статус: `sudo systemctl status docker`
- Включите автозапуск: `sudo systemctl enable docker`

### Проблема 4: "404 Not Found" при проверке сервера (на сервере)

**Причина:** Сервер еще не полностью запустился или неправильный формат URL.

**Решение:**

1. **Подождите 1-2 минуты** после запуска контейнера - серверу нужно время для инициализации

2. **Проверьте логи в реальном времени:**

   ```bash
   docker logs -f telegram-bot-api
   ```

   Должны появиться сообщения о запуске сервера. Нажмите Ctrl+C чтобы выйти.

3. **Проверьте правильность URL:**

   - Должен быть формат: `http://localhost:8081/botTOKEN/getMe`
   - Убедитесь, что нет лишних пробелов в токене
   - Убедитесь, что токен полный (включает двоеточие)

4. **Попробуйте другой endpoint:**

   ```bash
   curl http://localhost:8081/botYOUR_BOT_TOKEN/getUpdates
   ```

5. **Перезапустите контейнер:**

   ```bash
   docker restart telegram-bot-api
   ```

   Подождите 1-2 минуты и попробуйте снова.

6. **Проверьте, что контейнер работает:**
   ```bash
   docker ps
   docker exec telegram-bot-api ps aux
   ```

### Проблема 5: "Invalid api_id or api_hash" (на сервере)

**Причина:** Неправильные API credentials.

**Решение:**

1. Проверьте, что вы правильно скопировали api_id и api_hash с ПК
2. Убедитесь, что нет лишних пробелов в команде на сервере
3. Попробуйте получить новые credentials на ПК (шаг 1.1)

### Проблема 6: Бот не видит локальный API (на ПК)

**Причина:** Неправильный URL в `.env` на ПК или сервер недоступен.

**Решение:**

1. На сервере проверьте, что сервер запущен: `docker ps`
2. На ПК проверьте URL в `.env`: должно быть `http://SERVER_IP:8081` (не localhost, если сервер на другом компьютере!)
3. Проверьте логи бота на ПК при запуске
4. Попробуйте проверить сервер вручную с ПК (шаг 2.3.2)

### Проблема 7: "Connection refused" или "Connection timeout" (на ПК)

**Причина:** Сервер недоступен с ПК.

**Решение:**

1. На сервере проверьте, что контейнер запущен: `docker ps`
2. На сервере проверьте логи: `docker logs telegram-bot-api`
3. На сервере проверьте firewall - порт 8081 должен быть открыт:
   ```bash
   sudo ufw allow 8081
   ```
   или для iptables:
   ```bash
   sudo iptables -A INPUT -p tcp --dport 8081 -j ACCEPT
   ```
4. На ПК проверьте, что IP адрес сервера правильный
5. Убедитесь, что ПК и сервер в одной сети (или сервер доступен из интернета)

### Проблема 8: Файлы всё ещё не скачиваются больше 20MB (на ПК)

**Причина:** Бот не использует локальный API.

**Решение:**

1. На ПК проверьте логи бота при запуске - должно быть сообщение об использовании локального API
2. На ПК убедитесь, что `.env` файл сохранен и перезапущен бот
3. На ПК проверьте, что переменная правильно загружается
4. На сервере проверьте, что сервер работает: `curl http://localhost:8081/botTOKEN/getMe`

---

## 📊 Проверка работы с большими файлами

После настройки попробуйте:

1. **На ПК:** Отправьте боту видео файл размером больше 20MB (но меньше 2GB)
2. **На ПК:** Бот должен успешно скачать и обработать его
3. **На ПК:** Проверьте логи бота - не должно быть ошибок о размере файла
4. **На сервере:** Проверьте логи Docker контейнера: `docker logs telegram-bot-api`

---

## 🔒 Безопасность

⚠️ **Важные рекомендации:**

1. **На ПК:** Не публикуйте api_id и api_hash в открытом доступе
2. **На ПК:** Не коммитьте `.env` файл в Git (он уже в `.gitignore`)
3. **На сервере:** Если сервер доступен из интернета:
   - Используйте firewall для ограничения доступа к порту 8081
   - Настройте reverse proxy (nginx) с HTTPS
   - Используйте сильные пароли

---

## 📚 Дополнительная информация

- **Официальная документация Telegram Bot API:** https://core.telegram.org/bots/api
- **GitHub репозиторий telegram-bot-api:** https://github.com/tdlib/telegram-bot-api
- **Docker образ aiogram:** https://hub.docker.com/r/aiogram/telegram-bot-api

---

## ✅ Чек-лист настройки

### На ПК:

- [ ] Получены api_id и api_hash с https://my.telegram.org/apps
- [ ] Получен токен бота от @BotFather
- [ ] Добавлен `TELEGRAM_LOCAL_API_URL` в `.env` (с правильным IP сервера)
- [ ] Перезапущен бот
- [ ] В логах видно сообщение об использовании локального API
- [ ] Протестирована работа с файлом больше 20MB

### На сервере:

- [ ] Установлен Docker
- [ ] Запущен Docker контейнер с Bot API сервером (с api_id и api_hash)
- [ ] Проверена работа сервера через curl
- [ ] Узнан IP адрес сервера
- [ ] Открыт порт 8081 в firewall (если нужно)
- [ ] Контейнер настроен на автозапуск (`--restart unless-stopped`)

---

## 🎉 Готово!

Если все шаги выполнены:

- На сервере запущен локальный Bot API сервер
- На ПК бот настроен для работы с этим сервером
- Вы можете работать с видео файлами до 2GB!

Если возникли проблемы:

- **Проблемы с сервером:** проверьте логи на сервере: `docker logs telegram-bot-api`
- **Проблемы с ботом:** проверьте логи на ПК при запуске `bot.py`

---

## 📝 Краткая памятка: где что делать

| Действие                   | Где выполнять                |
| -------------------------- | ---------------------------- |
| Получить api_id и api_hash | **ПК** (браузер)             |
| Установить Docker          | **Сервер**                   |
| Запустить Bot API сервер   | **Сервер**                   |
| Узнать IP адрес сервера    | **Сервер**                   |
| Настроить `.env` файл      | **ПК**                       |
| Запустить бота             | **ПК**                       |
| Проверить работу           | **ПК** (отправить файл боту) |
