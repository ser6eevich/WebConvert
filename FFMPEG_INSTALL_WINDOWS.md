# Подробная инструкция по установке FFmpeg на Windows

## Способ 1: Ручная установка (рекомендуется)

### Шаг 1: Скачивание FFmpeg

1. Перейдите на один из сайтов:

   - **Рекомендуется**: https://www.gyan.dev/ffmpeg/builds/ (готовые сборки для Windows)
   - Альтернатива: https://ffmpeg.org/download.html

2. На сайте gyan.dev:
   - Найдите раздел "Release Builds"
   - Скачайте файл **`ffmpeg-release-essentials.zip`** (это полная версия со всеми необходимыми компонентами)
   - Или скачайте последнюю версию из раздела "Latest Release Builds"

### Шаг 2: Распаковка

1. Распакуйте скачанный ZIP-архив в удобное место

   - Например: `C:\ffmpeg`
   - Или: `C:\Program Files\ffmpeg`
   - Или: `D:\tools\ffmpeg`

2. После распаковки у вас должна быть структура папок примерно такая:
   ```
   C:\ffmpeg\
   └── ffmpeg-6.x-essentials_build\
       ├── bin\
       │   ├── ffmpeg.exe
       │   ├── ffplay.exe
       │   └── ffprobe.exe
       ├── doc\
       └── presets\
   ```

### Шаг 3: Добавление в PATH (переменные среды)

#### Вариант A: Через графический интерфейс

1. **Откройте окно "Переменные среды":**

   - Нажмите `Win + R`
   - Введите `sysdm.cpl` и нажмите Enter
   - Или: Правой кнопкой мыши на "Этот компьютер" → "Свойства" → "Дополнительные параметры системы"

2. **Откройте настройки переменных:**

   - В открывшемся окне перейдите на вкладку **"Дополнительно"**
   - Нажмите кнопку **"Переменные среды"** внизу окна

3. **Найдите переменную Path:**

   - В разделе **"Системные переменные"** (нижняя часть окна)
   - Найдите переменную `Path` в списке
   - Выделите её и нажмите кнопку **"Изменить"**

4. **Добавьте путь к FFmpeg:**

   - В открывшемся окне нажмите **"Создать"**
   - Вставьте путь к папке `bin` внутри распакованной папки FFmpeg
   - Например: `C:\ffmpeg\ffmpeg-6.x-essentials_build\bin`
   - **Важно:** Добавляйте именно папку `bin`, где находятся файлы `ffmpeg.exe`, `ffprobe.exe` и т.д.
   - Нажмите **"ОК"**

5. **Сохраните изменения:**
   - Нажмите **"ОК"** во всех открытых окнах

#### Вариант B: Через PowerShell (для продвинутых пользователей)

1. Откройте PowerShell **от имени администратора** (правой кнопкой → "Запуск от имени администратора")

2. Выполните команду (замените путь на ваш):
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\ffmpeg\ffmpeg-6.x-essentials_build\bin", [EnvironmentVariableTarget]::Machine)
   ```

### Шаг 4: Проверка установки

1. **Важно:** Закройте все открытые окна PowerShell/командной строки и откройте новое

   - Это необходимо, чтобы изменения в PATH вступили в силу

2. Откройте новое окно PowerShell или командной строки

3. Выполните команду:

   ```powershell
   ffmpeg -version
   ```

4. Если вы видите информацию о версии FFmpeg (например, "ffmpeg version 6.x..."), значит установка прошла успешно! ✅

5. Также можно проверить другие команды:
   ```powershell
   ffprobe -version
   ```

## Способ 2: Установка через Chocolatey (для продвинутых пользователей)

Если у вас установлен Chocolatey (менеджер пакетов для Windows):

1. Откройте PowerShell **от имени администратора**

2. Выполните команду:

   ```powershell
   choco install ffmpeg
   ```

3. FFmpeg будет автоматически установлен и добавлен в PATH

## Способ 3: Установка через winget (Windows 10/11)

1. Откройте PowerShell или командную строку

2. Выполните команду:
   ```powershell
   winget install ffmpeg
   ```

## Решение проблем

### Проблема: "ffmpeg не распознается как команда"

**Решение:**

1. Убедитесь, что вы добавили правильный путь (папка `bin`, а не корневая папка)
2. Закройте и откройте заново все окна PowerShell/командной строки
3. Перезагрузите компьютер (иногда требуется для применения изменений PATH)
4. Проверьте путь вручную: откройте папку `bin` и убедитесь, что там есть `ffmpeg.exe`

### Проблема: "Не удается найти указанный путь"

**Решение:**

1. Проверьте, что путь к папке `bin` правильный
2. Убедитесь, что папка существует и в ней есть файлы `ffmpeg.exe`, `ffprobe.exe`

### Проблема: Ошибки при конвертации видео

**Решение:**

1. Убедитесь, что вы скачали полную версию (essentials или full build)
2. Проверьте, что FFmpeg работает: `ffmpeg -version`
3. Попробуйте конвертировать видео вручную для проверки:
   ```powershell
   ffmpeg -i input.mp4 -vf scale=1920:1080 output.mp4
   ```

## Дополнительная информация

- **Официальный сайт FFmpeg:** https://ffmpeg.org/
- **Документация:** https://ffmpeg.org/documentation.html
- **Готовые сборки для Windows:** https://www.gyan.dev/ffmpeg/builds/

После успешной установки FFmpeg вы сможете использовать бота для конвертации видео! 🎬
