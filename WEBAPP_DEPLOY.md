# 📖 Инструкция по развертыванию Web App для загрузки видео

Эта инструкция поможет вам развернуть backend для загрузки видео через Telegram Web App на Ubuntu-сервере.

> 💡 **Для начинающих:** Если вы впервые развертываете сервер, начните с файла `DEPLOY_FOR_BEGINNERS.md` - там подробно объяснено, как получить сервер и домен.

## 📋 Содержание

1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Запуск backend сервера](#запуск-backend-сервера)
5. [Настройка Nginx](#настройка-nginx)
6. [Настройка SSL сертификата (Let's Encrypt)](#настройка-ssl-сертификата-lets-encrypt)
7. [Настройка бота](#настройка-бота)
8. [Автозапуск через systemd](#автозапуск-через-systemd)

---

## 🔧 Требования

- Ubuntu 20.04 или новее
- Python 3.8 или новее
- Root доступ или sudo права
- Доменное имя, указывающее на ваш сервер

---

## 📦 Установка зависимостей

### 1. Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 2. Установка Python и pip

```bash
sudo apt install -y python3 python3-pip python3-venv
```

### 3. Установка Nginx

```bash
sudo apt install -y nginx
```

### 4. Установка Certbot (для SSL)

```bash
sudo apt install -y certbot python3-certbot-nginx
```

---

## ⚙️ Настройка переменных окружения

### 1. Создайте директорию для проекта

```bash
cd /opt
sudo mkdir -p video-upload-webapp
sudo chown $USER:$USER video-upload-webapp
cd video-upload-webapp
```

### 2. Скопируйте файлы backend

Скопируйте папку `webapp/` из вашего проекта в `/opt/video-upload-webapp/`:

```bash
# Если вы на сервере, используйте scp или git
# Например:
scp -r webapp/ user@your-server:/opt/video-upload-webapp/
```

### 3. Создайте виртуальное окружение

```bash
cd /opt/video-upload-webapp
python3 -m venv venv
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install -r webapp/requirements.txt
```

### 5. Создайте директорию для видео

```bash
mkdir -p videos
chmod 755 videos
```

### 6. Создайте файл `.env`

```bash
nano .env
```

Добавьте следующие переменные:

```env
# Публичный URL вашего домена (БЕЗ слеша в конце!)
PUBLIC_BASE_URL=https://your-domain.com

# Директория для сохранения видео (относительный путь или абсолютный)
VIDEOS_DIR=videos

# Порт для backend сервера (обычно 8000)
WEBAPP_PORT=8000

# Максимальный размер файла в байтах (по умолчанию 2GB)
MAX_FILE_SIZE=2147483648
```

**Важно:**

- `PUBLIC_BASE_URL` должен быть HTTPS URL вашего домена (например: `https://my-domain.com`)
- НЕ добавляйте слеш в конце URL!
- `VIDEOS_DIR` может быть относительным (относительно `webapp/`) или абсолютным путем

---

## 🚀 Запуск backend сервера

### Вариант 1: Запуск вручную (для тестирования)

```bash
cd /opt/video-upload-webapp
source venv/bin/activate
cd webapp
python server.py
```

Сервер запустится на `http://0.0.0.0:8000`

### Вариант 2: Запуск через uvicorn (рекомендуется)

```bash
cd /opt/video-upload-webapp
source venv/bin/activate
cd webapp
uvicorn server:app --host 0.0.0.0 --port 8000
```

### Вариант 3: Запуск через gunicorn (для продакшена)

```bash
pip install gunicorn
cd /opt/video-upload-webapp
source venv/bin/activate
cd webapp
gunicorn server:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

---

## 🌐 Настройка Nginx

### 1. Создайте конфигурационный файл Nginx

```bash
sudo nano /etc/nginx/sites-available/video-upload
```

Добавьте следующую конфигурацию:

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # Логи
    access_log /var/log/nginx/video-upload-access.log;
    error_log /var/log/nginx/video-upload-error.log;

    # Максимальный размер загружаемого файла (2GB)
    client_max_body_size 2G;
    client_body_timeout 300s;

    # Проксирование на backend
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты для больших файлов
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Раздача статических видео файлов напрямую через Nginx (опционально, но быстрее)
    location /videos/ {
        alias /opt/video-upload-webapp/videos/;

        # Заголовки для видео
        add_header Content-Type video/mp4;
        add_header Accept-Ranges bytes;

        # Кэширование
        expires 30d;
        add_header Cache-Control "public, immutable";

        # CORS (если нужно)
        add_header Access-Control-Allow-Origin *;
    }
}
```

**Важно:** Замените `your-domain.com` на ваш домен!

### 2. Активируйте конфигурацию

```bash
sudo ln -s /etc/nginx/sites-available/video-upload /etc/nginx/sites-enabled/
```

### 3. Проверьте конфигурацию Nginx

```bash
sudo nginx -t
```

Если всё правильно, вы увидите:

```
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

### 4. Перезапустите Nginx

```bash
sudo systemctl restart nginx
```

---

## 🔒 Настройка SSL сертификата (Let's Encrypt)

### 1. Получите SSL сертификат

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

Certbot автоматически:

- Получит SSL сертификат
- Настроит Nginx для использования HTTPS
- Настроит автоматическое обновление сертификата

### 2. Проверьте автоматическое обновление

```bash
sudo certbot renew --dry-run
```

---

## 🤖 Настройка бота

### 1. Обновите файл `.env` в директории бота

Добавьте переменную `VIDEO_WEBAPP_URL`:

```env
# ... другие переменные ...

# URL для Web App загрузки видео
VIDEO_WEBAPP_URL=https://your-domain.com/upload
```

**Важно:**

- URL должен быть HTTPS (Telegram требует HTTPS для Web App)
- URL должен указывать на `/upload` (это страница загрузки)

### 2. Перезапустите бота

Если бот запущен через systemd:

```bash
sudo systemctl restart telegram-bot
```

Если бот запущен через pm2:

```bash
pm2 restart bot
```

Если бот запущен вручную, просто перезапустите его.

---

## 🔄 Автозапуск через systemd

### 1. Создайте systemd service файл

```bash
sudo nano /etc/systemd/system/video-upload-webapp.service
```

Добавьте следующее:

```ini
[Unit]
Description=Video Upload WebApp Backend
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/opt/video-upload-webapp/webapp
Environment="PATH=/opt/video-upload-webapp/venv/bin"
ExecStart=/opt/video-upload-webapp/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `your-username` на ваше имя пользователя!

### 2. Активируйте и запустите сервис

```bash
sudo systemctl daemon-reload
sudo systemctl enable video-upload-webapp
sudo systemctl start video-upload-webapp
```

### 3. Проверьте статус

```bash
sudo systemctl status video-upload-webapp
```

### 4. Просмотр логов

```bash
sudo journalctl -u video-upload-webapp -f
```

---

## 📝 Проверка работы

### 1. Проверьте backend

```bash
curl http://localhost:8000/health
```

Должен вернуться JSON:

```json
{ "status": "ok", "videos_dir": "/opt/video-upload-webapp/videos" }
```

### 2. Проверьте через домен

Откройте в браузере: `https://your-domain.com/upload`

Должна открыться страница загрузки видео.

### 3. Проверьте в Telegram боте

1. Откройте бота в Telegram
2. Нажмите кнопку "🎬 Загрузить видео"
3. Должна открыться Web App страница загрузки

---

## 🛠️ Устранение неполадок

### Проблема: Backend не запускается

**Решение:**

1. Проверьте логи: `sudo journalctl -u video-upload-webapp -n 50`
2. Проверьте, что порт 8000 свободен: `sudo netstat -tulpn | grep 8000`
3. Проверьте права доступа к директории `videos/`

### Проблема: Nginx возвращает 502 Bad Gateway

**Решение:**

1. Проверьте, что backend запущен: `sudo systemctl status video-upload-webapp`
2. Проверьте конфигурацию Nginx: `sudo nginx -t`
3. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/video-upload-error.log`

### Проблема: Файлы не загружаются

**Решение:**

1. Проверьте `client_max_body_size` в Nginx (должно быть 2G или больше)
2. Проверьте права доступа к директории `videos/`: `chmod 755 videos`
3. Проверьте логи backend: `sudo journalctl -u video-upload-webapp -f`

### Проблема: Web App не открывается в Telegram

**Решение:**

1. Убедитесь, что используется HTTPS (не HTTP)
2. Проверьте, что домен правильно настроен и указывает на сервер
3. Проверьте, что в `.env` бота указан правильный `VIDEO_WEBAPP_URL`

---

## 📊 Мониторинг и обслуживание

### Очистка старых видео

Создайте скрипт для удаления файлов старше N дней:

```bash
#!/bin/bash
# cleanup-old-videos.sh
find /opt/video-upload-webapp/videos -type f -mtime +30 -delete
```

Добавьте в crontab:

```bash
crontab -e
# Добавьте строку:
0 2 * * * /opt/video-upload-webapp/cleanup-old-videos.sh
```

### Резервное копирование

Рекомендуется настроить регулярное резервное копирование директории `videos/`.

---

## ✅ Итоговый чеклист

- [ ] Python 3.8+ установлен
- [ ] Nginx установлен и настроен
- [ ] SSL сертификат получен (HTTPS работает)
- [ ] Backend запущен и работает
- [ ] Директория `videos/` создана с правильными правами
- [ ] Переменные окружения настроены
- [ ] `VIDEO_WEBAPP_URL` добавлен в `.env` бота
- [ ] Бот перезапущен
- [ ] Web App открывается в Telegram
- [ ] Загрузка видео работает
- [ ] Автозапуск через systemd настроен

---

## 📞 Поддержка

Если возникли проблемы, проверьте:

1. Логи backend: `sudo journalctl -u video-upload-webapp -f`
2. Логи Nginx: `sudo tail -f /var/log/nginx/video-upload-error.log`
3. Логи бота (в зависимости от способа запуска)

---

**Готово!** Теперь ваш бот может принимать видео файлы любого размера через Web App! 🎉
