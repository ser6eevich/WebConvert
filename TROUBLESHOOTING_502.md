# 🔧 Устранение ошибки 502 Bad Gateway

Ошибка 502 означает, что Nginx не может подключиться к backend серверу на порту 8000.

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте, запущен ли backend сервис

```bash
sudo systemctl status webapp
```

Или если используете другое имя:

```bash
sudo systemctl status video-upload-webapp
```

**Если сервис не запущен или показывает ошибку:**

1. Посмотрите логи:

   ```bash
   sudo journalctl -u webapp -n 50
   ```

2. Проверьте, что backend может запуститься вручную:
   ```bash
   cd ~/video-upload-webapp/webapp
   source ~/video-upload-webapp/venv/bin/activate
   python server.py
   ```

### Шаг 2: Проверьте, слушает ли backend на порту 8000

```bash
sudo netstat -tulpn | grep 8000
```

Или:

```bash
sudo ss -tulpn | grep 8000
```

**Должно быть что-то вроде:**

```
tcp  0  0  0.0.0.0:8000  0.0.0.0:*  LISTEN  12345/python
```

**Если порт не слушается:**

1. Проверьте, что backend запущен
2. Проверьте логи на ошибки
3. Убедитесь, что порт 8000 не занят другим процессом

### Шаг 3: Проверьте конфигурацию Nginx

```bash
sudo nginx -t
```

Должно быть: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

### Шаг 4: Проверьте логи Nginx

```bash
sudo tail -f /var/log/nginx/video-upload-error.log
```

Там будут подробности ошибки подключения.

## 🔧 Решения

### Решение 1: Запустите backend сервис

Если сервис не запущен:

```bash
# Проверьте имя сервиса
sudo systemctl list-units | grep webapp

# Запустите сервис
sudo systemctl start webapp

# Или если другое имя:
sudo systemctl start video-upload-webapp

# Проверьте статус
sudo systemctl status webapp
```

### Решение 2: Исправьте service файл

Проверьте путь к виртуальному окружению и файлам:

```bash
sudo nano /etc/systemd/system/webapp.service
```

Убедитесь, что пути правильные (структура: `WebConvert/webapp`):

```ini
[Service]
Type=simple
User=root
WorkingDirectory=/root/WebConvert/webapp
Environment="PATH=/root/WebConvert/webapp/venv/bin"
ExecStart=/root/WebConvert/webapp/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000
```

**Важно:**

- Если вы используете `root`, пути должны быть `/root/WebConvert/webapp/...`
- Если используете другого пользователя, замените `/root` на правильный путь (например, `/home/ваш-пользователь`)
- Проверьте, что виртуальное окружение существует по указанному пути

После изменений:

```bash
sudo systemctl daemon-reload
sudo systemctl restart webapp
```

### Решение 3: Проверьте .env файл

```bash
cat ~/WebConvert/webapp/.env
```

Убедитесь, что:

- `PUBLIC_BASE_URL` указан правильно
- `VIDEOS_DIR` существует
- `WEBAPP_PORT=8000`

### Решение 4: Запустите backend вручную для теста

```bash
cd ~/WebConvert/webapp
source venv/bin/activate
python server.py
```

Если запускается, проверьте в другом терминале:

```bash
curl http://localhost:8000/health
```

Должен вернуться JSON.

### Решение 5: Проверьте права доступа

```bash
# Проверьте права на директорию
ls -la ~/WebConvert/webapp/

# Проверьте права на videos
ls -la ~/WebConvert/webapp/videos/
```

Если нужно, исправьте:

```bash
chmod 755 ~/WebConvert/webapp/videos/
```

## 📋 Быстрая проверка всех компонентов

Выполните эти команды по порядку:

```bash
# 1. Проверка статуса backend
sudo systemctl status webapp

# 2. Проверка порта 8000
sudo netstat -tulpn | grep 8000

# 3. Проверка конфигурации Nginx
sudo nginx -t

# 4. Проверка статуса Nginx
sudo systemctl status nginx

# 5. Проверка локального подключения к backend
curl http://localhost:8000/health

# 6. Проверка логов backend
sudo journalctl -u webapp -n 20

# 7. Проверка логов Nginx
sudo tail -20 /var/log/nginx/video-upload-error.log
```

## 🎯 Типичные проблемы и решения

### Проблема: "Connection refused"

**Причина:** Backend не запущен или не слушает на 0.0.0.0:8000

**Решение:**

1. Убедитесь, что backend запущен: `sudo systemctl start webapp`
2. Проверьте, что в `server.py` или service файле указан `--host 0.0.0.0`

### Проблема: "Permission denied"

**Причина:** Неправильные права доступа

**Решение:**

```bash
chmod 755 ~/video-upload-webapp/videos/
chown -R root:root ~/video-upload-webapp/
```

### Проблема: "ModuleNotFoundError"

**Причина:** Зависимости не установлены в виртуальном окружении

**Решение:**

```bash
cd ~/video-upload-webapp/webapp
source ~/video-upload-webapp/venv/bin/activate
pip install -r requirements.txt
```

### Проблема: Неправильные пути в service файле

**Причина:** Пути указаны для другого пользователя

**Решение:**

1. Узнайте текущего пользователя: `whoami`
2. Узнайте домашнюю директорию: `echo $HOME`
3. Обновите service файл с правильными путями
4. Выполните: `sudo systemctl daemon-reload && sudo systemctl restart webapp`

## ✅ После исправления

1. Проверьте статус:

   ```bash
   sudo systemctl status webapp
   ```

2. Проверьте порт:

   ```bash
   sudo netstat -tulpn | grep 8000
   ```

3. Проверьте локально:

   ```bash
   curl http://localhost:8000/health
   ```

4. Проверьте через домен:
   ```bash
   curl https://ikurganskiy.ru/health
   ```

Должен вернуться JSON: `{"status":"ok","videos_dir":"..."}`
