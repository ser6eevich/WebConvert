# 🔧 Исправление: Страница /files не отображается

Если страница `/files` не открывается, выполните следующие шаги:

## 🔍 Диагностика

### 1. Проверьте, что endpoint зарегистрирован

На сервере проверьте код:

```bash
# Проверьте, что endpoint /files есть в коде
grep -n "@app.get.*files" /root/WebConvert/webapp/server.py
```

Должна быть строка: `@app.get("/files", response_class=HTMLResponse)`

### 2. Проверьте статус webapp

```bash
# Проверьте статус сервиса
sudo systemctl status webapp

# Проверьте логи
sudo journalctl -u webapp -n 50
```

### 3. Проверьте доступность через curl

```bash
# Проверьте напрямую через backend (порт 8000)
curl http://localhost:8000/files

# Проверьте через домен
curl https://ваш-домен.com/files
```

## 🔧 Решение

### Шаг 1: Перезапустите webapp

```bash
# Перезапустите сервис
sudo systemctl restart webapp

# Проверьте статус
sudo systemctl status webapp

# Проверьте логи на ошибки
sudo journalctl -u webapp -f
```

### Шаг 2: Проверьте, что код обновлен

```bash
# Убедитесь, что вы в правильной директории
cd /root/WebConvert/webapp

# Проверьте, что endpoint /files есть
grep -A 5 "@app.get.*files" server.py
```

Должен быть виден код:

```python
@app.get("/files", response_class=HTMLResponse)
async def files_list():
    """
    GET /files - отображает HTML страницу со списком всех видео файлов
    """
```

### Шаг 3: Проверьте Nginx конфигурацию

```bash
# Проверьте конфигурацию Nginx
sudo nginx -t

# Проверьте, что все маршруты проксируются
sudo cat /etc/nginx/sites-available/video-upload | grep -A 10 "location /"
```

Должна быть секция:

```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    ...
}
```

### Шаг 4: Перезапустите Nginx (если нужно)

```bash
sudo systemctl restart nginx
```

## ✅ Проверка

1. **Откройте в браузере:**

   ```
   https://ваш-домен.com/files
   ```

2. **Или через curl:**

   ```bash
   curl -I https://ваш-домен.com/files
   ```

   Должен вернуться статус `200 OK` и `Content-Type: text/html`

3. **Проверьте через Web App:**
   - Откройте Web App через кнопку "🎬 Загрузить видео" в боте
   - Нажмите кнопку "📁 Все файлы"
   - Должна открыться страница со списком файлов

## 🐛 Если проблема сохраняется

1. **Проверьте логи webapp:**

   ```bash
   sudo journalctl -u webapp -n 100 | grep -i error
   ```

2. **Проверьте, что FastAPI видит все маршруты:**

   ```bash
   # Запустите webapp вручную для проверки
   cd /root/WebConvert/webapp
   source venv/bin/activate
   python server.py
   ```

   В логах должны быть все зарегистрированные маршруты.

3. **Проверьте синтаксис Python:**

   ```bash
   cd /root/WebConvert/webapp
   python -m py_compile server.py
   ```

4. **Проверьте доступность порта 8000:**
   ```bash
   sudo netstat -tulpn | grep 8000
   ```

## 📝 Примечания

- Endpoint `/files` должен быть зарегистрирован ДО endpoint `/health` в коде
- FastAPI обрабатывает маршруты в порядке их определения
- Если есть конфликт маршрутов, более специфичные должны быть определены раньше
